---
name: project-area-membros-quirk
description: "Área de membros própria pros clientes Quirk acessarem as mentorias (hoje no Drive); Next.js+Payload, 3 fases"
metadata: 
  node_type: memory
  type: project
  originSessionId: 635d4787-0e22-45b2-b202-ef558aebae16
  modified: 2026-08-19T20:42:45.490Z
---

Área de membros própria da Quirk pra clientes acessarem as mentorias que hoje ficam soltas no Google Drive. Em `/Users/renanreal/area-membros-quirk/` (repo git novo).

**Tiers de acesso da equipe (03/ago/2026, 6589d58):** `ehSupervisorOuAcima` (admin/supervisor/administrativo) ≠ `ehGestaoFinanceira` (SÓ admin+administrativo; access de coleção `isAdminOuAdministrativo`). Regra do Renan: **supervisor é papel de TRÁFEGO** — não vê Financeiro, BI, Contratos nem o Comercial da Quirk (menu, views, aba Financeiro do hub, seção Contratos do Cadastro, coleções pagamentos/contratos/snapshot, rota snapshot e autorizarCrm do funil interno). Usar `ehGestaoFinanceira` pra qualquer feature de faturamento/contratos/comercial nova. Outras UI: bolinha da conta = AvatarConta (foto/iniciais via admin.avatar); Relatórios dos clientes com filtros gestor/status (FiltrosRelatorios + linhas com gestores no overview); dashboard+funil comercial 100% escuros.

**Decisões fechadas no brainstorming (jul/2026):**
- Build customizado (não plataforma pronta), com identidade Quirk no cliente E no admin.
- Stack: **Next.js + Payload CMS 3 + Postgres** (Neon/Supabase), deploy Vercel.
- Vídeos no **Vimeo** (privado, travado no domínio).
- **4 vertentes** fixas: Marketing, Vendas, Assessoria Comercial, Posicionamento & Social. Todo cliente vê as 4 (acesso único).
- Modelo: Vertente → Módulo → Aula (vídeo Vimeo + descrição + materiais) → Progresso.
- Login email+senha; **admin provisiona** cada cliente (campo `ativo`). Múltiplos admins ok.
- **Progresso no v1**: assistida + continue de onde parou + barra.

**Faseamento (planos sequenciais, cada um entrega software testável):**
- Fase 1 — Fundação + modelo de dados + admin — **CONCLUÍDA (jul/2026)**. Branch `fase-1-fundacao-admin`, 11 commits. Payload 3.86.0 + Next 16.2.6 + Postgres (Neon, região sa-east-1). 6 collections (Users c/ role admin/member+ativo, Media, Vertentes, Modulos, Aulas c/ vimeoId+materiais+status, ProgressoAula isolado por cliente). 9 testes de integração verdes contra o Neon. Seed das 4 vertentes (npm run seed). Branding Quirk no admin (custom.scss: azul #1D80FF, verde, Sora/Poppins, titleSuffix). Admin do Renan: contato@quirkgrowth.com.br (senha temporária — trocar).
- Fase 2 — Área do cliente + progresso — **CONCLUÍDA (jul/2026)**. Branch `fase-1-fundacao-admin` (mesmo branch, 8 commits F2). Rotas em `src/app/(frontend)`: `/login` (client, POST /api/users/login), grupo `(app)` com layout-guard (redirect /login), home (cards + continue de onde parou), `/vertente/[slug]`, `/aula/[id]` (player Vimeo iframe + materiais + marcar assistida + próxima). Libs: getPayloadClient, auth (payload.auth via cookie), conteudo (queries), progresso (marcarAssistida/percentualVertente/continueDeOndeParou/idsAssistidas), actions/progresso.actions. Tema em globals-quirk.css; webfont Tabler via CDN. 14 testes int verdes (5 arquivos). Verificado e2e no browser: cliente navega, player toca, marca assistida, barra 50%, continue de onde parou. ACESSOS ATUAIS (após reset de exemplo): admin contato@quirkgrowth.com.br / Quirk@05369392; cliente teste cliente@teste.com / cliente123. Conteúdo exemplo: módulo + 2 aulas em Vendas (vimeoId 76979871 e 22439234).
**DEPLOY (jul/2026): NO AR.** Hospedado no **Render** (plano free, região virginia, serviço `area-membros-quirk`), deploy via Blueprint `render.yaml` puxando do GitHub **Quirk-growth/area-membros-quirk** (repo privado, branch main; remote origin já configurado). URL pública: **https://area-membros-quirk.onrender.com**. Env vars DATABASE_URI + PAYLOAD_SECRET setadas no Render (sync:false). Node 22.11 via .node-version. **Plano Render (jul/2026): instância Starter US$7/mês — cold start ELIMINADO** (medido: /login 0,2s vs ~50s antes). ⚠️ **CORREÇÃO (05/ago/2026, verificado ao vivo — Settings→Instance Type=Free + banner de spin-down + Free Instance Hours 23/750 consumidas):** o serviço está de VOLTA em instância **Free** — o Starter foi revertido (downgrade). Cold start voltou (~50s pós-deploy/hibernação). Mitigação atual: keep-warm via **UptimeRobot** (monitor HTTP em `membros.quirkgrowth.com.br/api/health` a cada 5min, ativo há ~7 dias) — reduz sono por inatividade mas NÃO mata o cold start pós-deploy. Recomendação p/ produção com clientes: voltar pra Starter (US$7). → **RE-RESOLVIDO no mesmo dia (05/ago):** Renan subiu o **workspace pra Pro (US$25)** e a **instância do web service pra Starter** (verificado ao vivo: Settings→Instance Type=Starter, badge Starter no header, banner de spin-down sumiu, `/login` ~0,2s). **Cold start eliminado de novo.** Custo de infra agora ≈ **US$35/mês** (Pro 25 + Starter 7 + Neon ~3,5). ⚠️ **Distinção que já confundiu 2x:** subir o *workspace* pra Pro NÃO troca o *Instance Type* do serviço — tem que trocar a instância separadamente em Settings→Instance Type. Latência residual: app em Virginia + Neon em SP (~cross-region); mover o banco pra us-east se precisar acelerar. ⚠️ Distinção que confundiu o cliente: **plano do workspace** (Hobby $0 / Pro $25 / Scale $499) ≠ **tipo de instância** (Free $0 / Starter $7 / Standard $25 / Pro $85). Hobby é "$0 + compute costs", então dá pra ficar no Hobby e pagar só a instância. Pagos liberam: zero-downtime deploy, SSH, scaling, one-off jobs, discos persistentes, previews. Starter mantém 512 MB RAM (só sobe CPU 0.1→0.5); se faltar memória, próximo degrau é Standard $25 (2 GB).
⚠️ Latência: app em Virginia + Neon em São Paulo = ~2,4s na API (cross-region). Se precisar acelerar, mover o banco pra us-east. Push futuro pro GitHub → Render redeploya sozinho. Logo oficial já aplicado (login+header). **DOMÍNIO PRÓPRIO NO AR: https://membros.quirkgrowth.com.br** (HTTPS emitido pelo Render/Google Trust). ⚠️ LIÇÃO: o DNS do quirkgrowth.com.br é gerenciado no **Cloudflare** (NS mike/brenda.ns.cloudflare.com), NÃO no cPanel/StayDNS — qualquer registro DNS desse domínio se faz no Cloudflare. O CNAME `membros` → `area-membros-quirk.onrender.com` precisa ficar em **"Somente DNS" (nuvem cinza)**; com proxy laranja o Render não emite o certificado. (Há um CNAME órfão no Zone Editor do cPanel que pode ser apagado.)

**Onboarding por email (jul/2026):** decisão — cliente ativo vem do **ClickUp** (space "Clientes" → lista "Lista de clientes" id 901702066250; campos Email obrigatório id d9d24a58..., Nome Completo 944c964b..., Status dropdown 747fe5e5... com valores Onboarding/em atividade/Risco de Saída/Churn/Renovação/Stand By/SETUP). Regra: **todo status ≠ Churn/Stand By = tem acesso**. Email é disparado via **webhook do Make/n8n** (cliente já tem fluxo de email; NÃO tem SMTP direto). Construído e testado e2e: adaptador `src/email/webhookAdapter.ts` faz POST em `MAIL_WEBHOOK_URL` com {to,subject,html}; hook afterChange em Users manda email de boas-vindas (link /definir-senha?token=) ao criar member; helper único `src/email/enviarConvite.ts` (usado pelo hook e pela reativação); auth.forgotPassword customizado (7 dias) pro "esqueci senha"; páginas /definir-senha e /esqueci-senha; templates com marca Quirk em src/email/templates.ts.

**SINCRONIZAÇÃO ClickUp→área: NO AR E RODANDO (jul/2026, 84 clientes provisionados).** Endpoint `POST /api/sync/reconcile` (src/lib/reconcile.ts + reconcileDiff.ts + route em src/app/api/sync/reconcile/route.ts) protegido por header `x-sync-secret` (SYNC_SECRET; cópia em `~/.config/n8n-quirk/area_membros_sync_secret.txt`). Recebe {clientes:[{email,nome}],dryRun} e cria/reativa/desativa members; **trava anti-zeragem** (aborta se ativos-desejados < 50% dos atuais; SYNC_MIN_RATIO) — SALVOU quando a base estava poluída. Dois workflows no **n8n** (`https://n8n.quirkgrowth.online`, API key em `~/.config/n8n-quirk/api_key.txt`, helper `quirk_auto_ads/scripts/n8n_api.py`): **A "Disparador de Email"** id `1fhiClCwQnHPOyk1` (Webhook `/webhook/quirk-email` → Gmail cred `OfOKnGWUn7q0MgoM`; = MAIL_WEBHOOK_URL); **B "Reconciliador diário"** id `gAycGUV2KXrDNHVT` (Schedule 7h → HTTP ClickUp → Code parse → HTTP reconcile). ⚠️ **GOTCHAS resolvidos:** (1) API pública do n8n NÃO executa workflow (405) nem lista credenciais (403); testar via webhook temporário + ler /executions. (2) A cred ClickUp **OAuth** (`clickUpOAuth2Api` GOeWz2DVFUVqwDeM) NÃO é acessível a workflow criado via API ("does not exist"); a cred **token** (`clickUpApi` GAcUN3AJmRckkpjk "ClickUp account") FUNCIONA. (3) A lista de clientes tem status nativo "clientes ativos" tratado como *fechado* → precisa `include_closed=true`; e ~9 clientes são subtarefas → precisa **`subtasks=true`**, senão a query perde ~9. (4) O campo Status custom vem como **orderindex numérico** (0 Onboarding..6 SETUP); excluir 3 (Churn) e 5 (Stand By). Distribuição real jul/2026: 72 em atividade + 14 onboarding + 4 Churn + 5 StandBy = 86 ativos (84 emails únicos válidos).

**"Convidar cliente" no admin (jul/2026):** botão no `beforeListTable` de Users (`src/components/admin/ConvidarCliente.tsx`) → `POST /api/users/convidar` (endpoint da coleção, só admin; `src/lib/convidarCliente.ts`) cria member c/ senha aleatória → convite automático. Admin NÃO digita senha (o Payload exige senha no create padrão por `requirePassword=!id`, sem config; por isso a ação própria). "Create New" de usuários escondido via CSS (só convite). ⚠️ **Qualquer componente OU view custom novo no admin EXIGE regenerar o importMap** (`npm run generate:importmap`, ou `node_modules/.bin/payload generate:importmap` se o pnpm não estiver no PATH) senão TELA BRANCA — o `Component: '/components/...#default'` é resolvido por string via `src/app/(payload)/admin/importMap.js`, que é GERADO; registrar só no `payload.config` (admin.components.views) não basta. Repetiu em 07/ago: o `PessoaView` (rota `/admin/pessoa`) subiu sem o importMap regenerado e abria em branco em prod (rota existia = não era 404, o componente é que não resolvia); fix = regenerar + deploy (`3ab0dff`). Conferir depois de mergear worktrees: um rebase pode trazer o config novo mas um importMap velho (as views da agenda entraram, a minha não). ⚠️ **GOTCHA do `AvatarConta` (admin.avatar):** todo componente montado no `AvatarConta` (sino, ícone de chat, drawer…) vive **dentro do `<a href="/admin/account">`** que o Payload põe em volta do avatar. Clique lá borbulha até a âncora, cujo handler de nav SPA chama `preventDefault()` — o que **mata ações padrão** (submit de `<form>`, navegação de link) de qualquer interativo aninhado. Botões com `onClick` sobrevivem (o handler React roda); quem depende de submit NATIVO, não. Sintoma real (08/ago, chat): clicar "enviar" no drawer não mandava nada. ⚠️ **PEGADINHA DO PORTAL:** `createPortal` pro `document.body` move o DOM mas os **eventos SINTÉTICOS do React continuam borbulhando pela árvore de COMPONENTES** — então o clique ainda alcança o `onClick` do ancestral e o `preventDefault` dele ainda mata o submit. Portal SOZINHO NÃO resolve (confirmado ao vivo: stack do preventDefault vinha de um `onClick` React da âncora `app-header__account`). **O fix que resolve = `onClick={(e) => e.stopPropagation()}` no container do painel** (isola dos handlers React ancestrais). Chat usa portal (higiene, tira o interativo de dentro do `<a>`) **+ stopPropagation no `<aside>`** (o que de fato conserta). Deploy do fix: `2baa501` (portal, insuficiente) → `c8b8a7c` (stopPropagation, resolve). Vale pra qualquer painel/drawer novo montado no AvatarConta (CRM Conversas etc.).

**Banner da Home:** global `banner-home` editável no admin (ativo/título/subtítulo/botão+link/imagem). BannerHomeView renderiza grande no topo; se imagem SEM título → mostra a arte pronta limpa e clicável; senão gradiente Quirk + texto.

**Armazenamento de arquivos: RESOLVIDO (jul/2026) — Cloudflare R2.** `@payloadcms/storage-s3` no payload.config (collections: media). Bucket **privado** `quirk-membros` (Cloudflare R2, região auto/NA-East), endpoint `https://e1c74abadea36177b44269098ef72d74.r2.cloudflarestorage.com`. Token R2 "Area de Membros Quirk" com permissão mínima (leitura/gravação de objeto, só nesse bucket). Env vars: R2_BUCKET, R2_ENDPOINT, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY (local .env + Render). Verificado em produção: upload cai no bucket, não no disco.

⚠️ **ARMADILHA do Render:** a tela Environment **substitui a lista inteira** ao salvar — adicionar variáveis novas APAGOU DATABASE_URI/PAYLOAD_SECRET/NODE_VERSION e derrubou o app ("missing secret key" / "error initializing Payload"). Ao editar env vars, conferir que TODAS as 7 continuam lá. Também: `/login` é estática e responde 200 mesmo com o Payload quebrado — para saber se o app está REALMENTE no ar, testar a **API** (`POST /api/users/login`), não a página.

**Vimeo (jul/2026):** conta "Renan Real", plano **starter**, quota 2,19 TB livres. App API "Area de Membros Quirk" (id 537208) em developer.vimeo.com. Token pessoal precisa dos escopos public/private/create/edit/**upload**/video_files (+**delete** se for limpar). ✅ Confirmado por teste: upload via API funciona E **`privacy.embed:"whitelist"` (domain-lock) funciona no Starter** — vídeos privados travados em membros.quirkgrowth.com.br.

⚠️ **LIÇÃO GRAVE (jul/2026, reincidiu e foi corrigida DE VERDADE):** a suíte de testes escrevia no **banco de PRODUÇÃO** e poluiu a área com vertentes "V"/aulas/usuários fictícios. A 1ª "correção" (trava no `tests/helpers/testPayload.ts`) NÃO funcionava: o `payload.config` lê `DATABASE_URI` no **import** (buildConfig no load), então sobrescrever a var dentro do getTestPayload era TARDE DEMAIS — rodar `npm test` continuava escrevendo em produção. **Fix real (commitado):** mover a trava pra `vitest.setup.ts` (roda ANTES de qualquer import) — exige `DATABASE_URI_TEST`, recusa se == produção, e faz `process.env.DATABASE_URI = testUri` ali. Banco de teste descartável = **`neondb_test`** (mesmo endpoint Neon, db separado). Limpeza jul/2026: apagados 403 usuários de teste + 81 vertentes de teste (mantidos os 84 clientes reais + Marketing 9 aulas/Vendas 10 aulas). **Sempre verificar o estado real via SQL/API depois de operações em lote — relatório de subagente não mostra lixo pré-existente; e conferir PRA ONDE os testes escrevem antes de rodá-los num repo com DB de produção no .env.**

**Receita da migração (VALIDADA):** runbook em scratchpad/runbook.md. Drive público por link → `curl -sL "https://drive.usercontent.google.com/download?id=<ID>&export=download&confirm=t"` (funciona com arquivos grandes; conferir `file` = ISO Media, não HTML) → Vimeo tus (POST /me/videos approach=tus + PATCH bytes + PUT /videos/<id>/privacy/domains/membros.quirkgrowth.com.br) → POST /api/aulas. ⚠️ O conector MCP do Drive devolve arquivo como **base64 no contexto** — inútil pra vídeo (estoura). Usar curl direto. ⚠️ curl: usar `-g` com URLs que têm `[]`, e `--resolve` literal (não em variável).

**MIGRAÇÃO CONCLUÍDA (jul/2026): 19 aulas no ar** — Marketing 9 (módulo id 10) + Vendas 10 (módulo id 11), ambos "Mentorias gravadas". Só 6 tinham transcrição → 13 estão SEM descrição de propósito (não inventar). Duplicatas descartadas: "Linha editorial que converte"="Linha Editorial que Vende" (grav. 26/05) e "Mentoria Comercial Chay 24.03"="Atendimento e relacionamento 24.03" (arquivos byte-idênticos). Fora: pasta de prints/corretores, "Destravar 07.04" (só áudio), CAC duplicado 15.04.
✅ **Vimeo REPLACE funciona no Starter**: `POST /videos/<id>/versions` com tus — troca o arquivo mantendo o mesmo id (a aula não muda). Usado pra corrigir o vídeo do CPL (aula 17 / vimeo 1210588506).
⚠️ zsh faz brace expansion em JSON inline no curl — montar payload via arquivo/python, não inline.

**Migração do Drive:** biblioteca em "Mentoria Clientes (subir na green)" (folder id `1kMRHSFfRGCUrMTBLLkdZIkKfvagRyoWb`), ~17 subpastas datadas = 1 mentoria cada. Cada pasta tem gravação Zoom crua: `GMT*_Recording_as_*.mp4` (52–217 MB), `.m4a` e **`.transcript.vtt`** (usar a transcrição pra gerar a descrição "o que você vai aprender"). Total ~2 GB. ⚠️ Triar antes: algumas pastas podem ser mentoria INDIVIDUAL de cliente (Chay, Rodrigo Sant'ana, Brain) — como todo cliente vê tudo, individual NÃO pode entrar (vazaria dado de cliente).

- PENDÊNCIAS: rotacionar senha Neon (colada no chat) + deletar o PAT do GitHub após uso; logo Quirk falta só no admin do Payload; apagar 2 vídeos-fantasma de teste no Vimeo (ids 1210578373 e 1210578479); email transacional p/ reset senha (link existe, envio não); rotacionar senha do Neon (foi colada no chat); limpar docker-compose.yml (Mongo do template); responsivo mobile fino; deploy Vercel + domínio membros.quirkgrowth.com.br.

**Auditoria 360 (05/ago/2026):** notas — Segurança **7,0** (base forte, travada por 1 CRÍTICO), Espaço & Limites **8,5** (folga em tudo; único limite = instância Free do Render), Responsividade **7,0** (mobile-consciente, mas kanban não arrasta no toque + grids de config não colapsam por override inline). Scorecard visual salvo como artefato claude.ai. **CRÍTICO de isolamento (fere a regra de ouro [[feedback-isolamento-dados-clientes]]):** a coleção `Media` tem `read: isAuthenticated` SEM campo/filtro de cliente → qualquer usuário logado (até um vendedor de outro cliente) lista/baixa arquivos de TODOS os clientes via `GET /api/media` + `/api/media/file/<nome>` — inclusive **cartões de CNPJ** do cadastro público (R2 é servido pela rota do Payload, então o gate é só esse read). Fix: campo `cliente` em Media + read filtrado por tenant + mover docs sensíveis (CNPJ) pra coleção só-equipe. **Importante:** campos internos do `Clientes` (comissaoPct=take-rate, mrr/financeiro*, statusSatisfacao, nivelCliente, gestores, asaasCustomerId, squad/grupo*/pastaDrive*) voltam no `GET /api/clientes/<id>` do próprio member (`admin: hidden` NÃO trava a API) → precisam de `access.read` por campo (padrão do `crmWebhookToken`, Clientes.ts:834). Menores: webhook Meta fail-open sem META_APP_SECRET (deveria 503); `npm audit` 5 high (next/sharp); sem rate-limit nos webhooks públicos; SVG habilitado no upload (XSS armazenado); sem headers de segurança (HSTS/X-Frame-Options); gestao-rotina read não escopa gestor por carteira; progresso-aula create não força cliente=user.id. Base é sólida (porta única de autorização, clienteId sempre da sessão, timingSafeEqual em 100% dos secrets, anti-escalada por campo em Users, IDOR do Meta corrigido). **CRÍTICO RESOLVIDO (05/ago, deploy 37b0c1d):** Media ganhou `cliente` (relationship) + `restrito` (checkbox); read escopado em `src/access/mediaRead.ts` (equipe vê tudo; member só global/sem-cliente OU do próprio cliente, nunca restrito); CNPJ do cadastro vira `restrito:true`; teste unitário `tests/unit/mediaRead.spec.ts` (5/5); DDL prod aplicada+verificada no projeto odd-cake-72216314 (colunas + índice + backfill — 11 media, 0 CNPJ existentes ainda, então nada tinha vazado). **P1 FEITO (05/ago, deploy d244f7c):** (a) travas de leitura por campo no Clientes (`src/access/travasClientes.ts` — financeiro/comercial só admin+administrativo via `ehGestaoFinanceira`; operacional toda equipe via `ehEquipe`; aplicado por `Clientes.fields = comTravasDeLeitura(Clientes.fields)` no fim do arquivo, preserva o access próprio do crmWebhookToken; telas usam overrideAccess/SSR então não quebram; teste `tests/unit/travasClientes.spec.ts`); (b) SVG removido do upload (Media.mimeTypes explícitos, sem `image/*` + regex do cadastro); (c) headers de segurança no `next.config.ts` (HSTS, X-Frame-Options SAMEORIGIN, nosniff, Referrer-Policy); (d) webhook Meta fail-closed (503 se `META_APP_SECRET` ausente). AINDA PENDENTE: backfill de `cliente_id` nos logos dos clientes (Media já suporta), rate-limit nos webhooks públicos, upgrade de deps (5 CVE high next/sharp — risco de breaking, fazer com teste), CSP (exige tuning). **Responsividade P1 FEITO (05/ago, deploy a9b39f5):** mover lead por toque no mobile — na visão **Lista** a coluna Etapa virou `<select>` que chama `mover()` (reusa `chipEtapa`; `<td onClick stopPropagation>` pra não abrir a ficha junto); no celular (≤700px) o CRM **abre em Lista por padrão** via `useEffect` + `matchMedia` (sem mismatch de hidratação; só quando a `visao` não vem fixada pelo menu lateral); desktop mantém o drag do kanban. Arquivo `src/components/crm/CrmBoard.tsx`. Verificação de toque REAL = testar no celular (não dá pra montar local: /crm é autenticado + precisa de cliente com leads). Ainda aberto na responsividade: grids do admin financeiro em `repeat(4,1fr)` que não colapsam, viewport explícito no layout. ⚠️ eslint do projeto crasha na invocação direta por arquivo (config circular) — usar `npm run lint` ou confiar no lint do `next build`.

**Criar cobrança no Asaas a partir do Financeiro (deploy 06/ago, `12178b7`, main):** substitui o Make 3360976 (`[AHL] Criar Pagamento no Asaas`). Botão **"Criar cobrança"** na aba Financeiro (`HubAbaFinanceiro`, acima do gate de vínculo — aparece MESMO sem `asaasCustomerId`, pra 1ª cobrança do cliente novo; só admin/administrativo) → modal `CriarCobrancaModal.tsx` (assinatura recorrente ou avulsa; valor default `valorMensal`; Boleto/Pix/Cartão; ciclo/parcelas/vencimento) → `POST /api/asaas/cobranca` (gestão financeira, fail-closed): garante customer (reusa `asaasCustomerId` OU busca por `externalReference=cli:ID` OU cria) → aplica **padrão Quirk de notificações** → cria assinatura/avulsa → `syncFinanceiro`. Camada `src/lib/asaas/api.ts` ganhou POST/PUT (`criarClienteAsaas`, `criarAssinaturaAsaas`, `criarCobrancaAsaas`, `aplicarNotificacoesPadrao`, `buscarClientePorRef`, `listarAssinaturasAsaas` já existia). Lógica pura testada: `src/lib/asaas/notificacoes.ts` (padrão = 3 eventos: `PAYMENT_DUEDATE_WARNING` offset 5→email, offset 0→email+whats, `PAYMENT_OVERDUE` offset 3→email+whats; **resto `enabled:false`**, SMS/provider off) + `src/lib/asaas/cobranca.ts` (payload/datas: assinatura nextDueDate=amanhã, avulsa dueDate=+3d). **Travas de billing** (review final da branch): não cria 2ª assinatura ACTIVE no mesmo cliente (409); modal trava re-clique após sucesso; reusa customer órfão por externalReference. Endpoint retroativo `POST /api/asaas/normalizar-notificacoes`. Campo `clientes.asaasAssinaturaId` (DDL aplicada em prod). Construído via SDD (7 tasks + review final opus + 5 fixes + re-review = Ready:Yes). **PENDENTE:** teste e2e (Renan cria uma cobrança REAL e confere) — nunca rodado pelo Claude (cria cobrança de verdade). Cliente-referência do padrão de notificação = Asaas `cus_000191318891` (DOM Imobiliários). ⚠️ Branch `feat/crm-filtros-funil-conversao` (filtros + funil de conversão na Visão geral do CRM) ficou COMPLETA mas NÃO-mergeada na main — loose end pré-existente, decidir depois.

**TEMA CLARO/ESCURO do Portal da Equipe + preferências por conta — NO AR (07/ago/2026, deploy bf2d901..d825176 → main).** Spec/plano `docs/superpowers/{specs,plans}/2026-08-06-prefs-usuario-tema-portal*`. (1) **Campo `users.preferencias`** (jsonb; DDL manual em prod) com `{tema, visualizacaoTarefas}`; grava SÓ via `POST /api/users/preferencias` (gate `ehEquipe`, merge raso validado em `src/lib/preferencias.ts`; field access `create/update:false` bloqueia PATCH direto). (2) **Rótulos nos filtros de Tarefas** + toggle Kanban/Lista que persiste por conta (`modoInicial` + `modoDeAbertura`; mobile força Lista sem gravar). (3) **Tema**: tokens `--pt-*` no topo do `custom.scss` (claro/escuro via `[data-tema]` no html), script no-flash inline no NavQuirk (cookie `quirk-tema`), **sol/lua `SeletorTema`** no rodapé do menu (persiste no banco + reconcilia cookie), varredura de ~90 arquivos (cores de DADO ficam literais). Menu lateral e login SEMPRE como hoje. **Área do cliente fica IDÊNTICA**: os componentes CRM são COMPARTILHADOS com `(frontend)/(app)/crm`, então `globals-quirk.css` tem bloco de mapeamento `--pt-*`→vars legadas (incl. `--pt-acento-2: var(--cyan)` — no portal acento-2 = azul; cuidado: acento-2 como FUNDO com texto branco só funciona no portal). ⚠️ Lições: `.template-default__wrap` do Payload pinta branco por cima do template (tokenizar os dois); títulos h1-h4 do conteúdo herdam cinza fixo do Payload; verificação visual REAL (dev server no banco de teste via `diag-dev-teste.mjs`, login seed `verificacao-visual@quirk.test`) achou 3 bugs que review de código não viu. Componente novo no portal = usar `var(--pt-*)`, nunca hex de superfície; se for compartilhado com o cliente, mapear o token no `globals-quirk.css`. **Telas NATIVAS do Payload** (listas/edição de coleção, ex. Gestão de Rotina) são pintadas pelo tema interno dele: o sol/lua e o script no-flash sincronizam `data-theme` (dark/light) junto com `data-tema` (5feff91) — sem isso ficava branco-no-branco no escuro. **MENU LATERAL nasce FECHADO e lembra a escolha (07/ago, commit `25a6263` → main).** Toda seção do menu do admin abre fechada; o que a pessoa abre grava em `preferencias.menuSecoesAbertas` e vale entre telas/sessões. Guarda as ABERTAS (não as fechadas): quem nunca mexeu não tem a chave, e assim seção NOVA nasce fechada por padrão. O clique manda um **DELTA** (`{menuSecoes:{'Tráfego':true}}`), nunca a lista inteira — senão dois cliques rápidos se atropelam; o servidor mescla em `normalizarPreferencias`. `SecaoNav` (client) só intercepta o `toggle`; o `<details>` nativo segue fazendo o abre/fecha. Flag `recolhida` do `menuAdmin` foi REMOVIDA (virou redundante). Sem DDL — a coluna `preferencias` já existia. ⚠️ Efeito visível: no 1º acesso o menu aparece só com títulos até clicar. Melhoria não feita (avisar antes de fazer): abrir sozinha a seção da página atual. Pendências aceitas: teste HTTP dos 403/400 do endpoint; `lerTemaDoCookie` sem uso (regex duplicada no script inline); vars `--success/--muted/--dim` dangling pré-existentes em HubRelatorioForm/HubBriefingForm/HubAbaTarefas; olhar `/crm` como cliente pós-deploy (drift cosmético do dark).

**Gestão de Rotina — janela do relatório semanal (07/ago, 2183360):** 7 dias FECHADOS terminando na VÉSPERA da referência (ref 07/08 → 31/07..06/08; dado do dia corrente é incompleto nas plataformas). Vale na prévia da aba Rotina do Hub E no congelamento do salvar (`janelaSemana` exportada de src/collections/GestaoRotina.ts, teste tests/unit/janelaSemana.spec.ts).

**Direção visual APROVADA (jul/2026) — DNA da LP Quirk Tech** (`/Users/renanreal/lp-quirk-tech/index.html`): fundo quase preto-azulado `#05080f` (bg-2 `#070c1a`, card `#0a1530`), brilhos radiais azuis rgba(29,128,255,.16) nos cantos, textura de grade sutil rgba(140,190,255,.04), cards de vidro `linear-gradient(160deg,rgba(18,32,62,.7),rgba(8,14,28,.7))` borda rgba(140,190,255,.12), títulos com degradê `linear-gradient(96deg,#fff,#9fcaff,#1D80FF,#6fdcff)` (background-clip text), botão primário `linear-gradient(100deg,#3a93ff,#0a5fd4)`, verde `#39b54a`, ciano `#6fdcff`, texto `#eaf1ff`/soft `#9fb2d4`/muted-azul `#8CBEFF`. Fontes Sora (títulos) + Poppins (corpo). 3 telas mockadas e aprovadas: login, home (resume "continue de onde parou" + cards das vertentes c/ barra), página da aula (player 16:9 + materiais + lista de aulas c/ ✓).

**Como resetar o banco em dev:** node + pg → `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` depois `npm run seed`. Payload em dev faz *push* do schema; ALTER em tabela existente abre prompt interativo que trava testes — por isso resetar quando necessário.

**Trabalho paralelo em worktrees (06/ago/2026):** o projeto é tocado por vários chats simultâneos, cada um num **worktree do mesmo repo** (`area-membros-quirk`, `-acesso`, `-crm`, `-4`) — mesmo `.git`, então cada chat enxerga as branches dos outros sem precisar de push. O `-4` (`wip/chat-4`, saído de `main`) é o workspace curinga pra tarefa nova, com `BRIEFING.md` na raiz (não versionado) mapeando quem mexe em quê. Estado é volátil: conferir com `git worktree list` + `git branch -vv`, nunca assumir. ⚠️ Worktree novo nasce SEM `.env`, `test.env` e `node_modules` (nenhum é versionado) — copiar os envs e rodar `npm install`, senão nada roda. ⚠️ Branch que mexe em `src/components/admin/hub/*` colide feio com quem estiver no tema do portal (toca ~42 arquivos); tarefa de backend/coleção conflita no máximo em `payload.config.ts`/`payload-types.ts`. Ver [[feedback-memoria-multichat]] pro protocolo entre os chats.

**HOME REMODELADA — NO AR (07/ago/2026, mergeada na main).** A tela inicial saiu de "KPIs de anúncios + três cartões *em breve*" para **duas camadas**: (1) **fila de ação** pessoal "Precisa de você" — tarefas atrasadas/vencendo, lead parado, SLA estourado, reunião de hoje, pré-fechamento, cliente que entrou em Risco/Renovação, contrato sem assinatura; (2) **cartões de pulso** Tráfego/Comercial/Carteira, que resumem e encaminham pras telas dedicadas em vez de competir com elas. Fila com 3 estados (minimizada — some mas o contador fica; padrão 5; expandida 10). Agenda ficou como espaço reservado: os eventos vivem no Google e ler a agenda de cada pessoa é projeto próprio (o `agendaWebhook.ts` só ESCREVE lá, e a conexão do Make 3704159 está quebrada desde mai/26).

Arquitetura: seletores puros em `src/lib/home/*` (`acaoTarefas`/`acaoComercial`/`acaoClientes`/`acaoContratual`/`pulso`/`fila`/`regras`), componentes burros em `src/components/admin/home/`, e **`src/lib/home/painel.ts` (`widgetsDoPainel(role)`) como fonte única de quem vê o quê** — no mesmo idioma do `menuAdmin.ts`. Bloco não autorizado **nem é consultado** (segurança, não só filtragem no fim). `PainelInicialView` virou orquestrador de 69 linhas.

**Decisões de produto do Renan (não reverter sem falar com ele):**
- **NENHUM dado financeiro na home** — sem MRR, a receber, inadimplência ou valor de contrato/pipeline. **Exceção deliberada:** o cartão de Tráfego mostra "Investido R$", que é verba de campanha do CLIENTE, não faturamento da Quirk, e já era exibido antes.
- **Papel `comercial` passou a ter home** (antes era redirecionado direto pro /admin/comercial).
- **Contrato sem assinatura só é cobrado se criado nos últimos 30 dias** (`DIAS_CONTRATO_RECENTE`). Motivo concreto: produção tinha 12 contratos em aberto, **11 com +60 dias e o mais antigo de ago/2024** — sem janela, ele ficaria cravado em vermelho na posição #1 da home de todo admin para sempre, comendo 20% de uma fila de 5.
- **Etapa `ganhou` fica FORA da regra de "lead parado"** — pro Renan o contrato só fecha de verdade quando vai pro Hub, então `ganhou` é ganho, não lead parado. Segue contado no pulso como "Contratos do mês".
- Card "Social media" foi removido em vez de virar mais um "em breve".

**Campo novo `clientes.statusDesde`** (+ hook `carimbarStatusDesde` em beforeChange): carimba QUANDO o status mudou — `updatedAt` não serve, muda a cada edição de qualquer campo. DDL aplicada à mão em prod ANTES do deploy (push OFF em prod), `scripts/ddl/2026-08-06-status-desde.sql`. **Sem backfill de propósito** → o bloco "cliente mudou de estado" nasce vazio e enche sozinho. ⚠️ O hook roda em TODA escrita de cliente: se o código subisse sem a coluna, quebraria hub, sync, reconcile e onboarding, não só a home.

Construído por SDD (11 tasks TDD + revisão com mutation testing por tarefa + revisão final da branch inteira). Spec e plano em `docs/superpowers/`. 499 testes unitários + 4 de integração verdes.

**AGENDA DO TIME — NO AR (07/ago/2026, `feat/agenda-time` mergeada, deploy 93e2467).** Cada pessoa da equipe conecta a **URL secreta iCal** da própria agenda (Google: Configurações→Integrar agenda→Endereço secreto iCal; funciona igual no Outlook — 1 pessoa do time usa) em `/admin/minha-agenda` (item na engrenagem). **Por que iCal e não OAuth:** time é 14×gmail.com sem Workspace → app OAuth seria Externo com escopo sensível = verificação do Google (semanas) ou tokens expirando em 7 dias; iCal não expira e cobre o Outlook. Troca futura por OAuth = substituir a camada de eventos, telas ficam.
- **Privacidade (decisão do Renan):** cada um vê a própria; `ehSupervisorOuAcima` vê TODAS (tela `/admin/agenda-time`, linhas por pessoa, navegação ?dia= na janela de 7d, menu via TRAFEGO_GESTAO). Aviso na cara da pessoa ANTES de colar a URL: quem vê, o que é lido, desconectar apaga tudo (eventos ANTES do registro, pra falha parcial não deixar dado sensível órfão).
- **Arquitetura:** URL cifrada AES-256-GCM (chave própria `AGENDA_SECRET_KEY`, fail-closed, NUNCA PAYLOAD_SECRET — rotacionar não desloga) em coleção `agendas` (1/pessoa, NÃO em Users — payload.update em Users desloga); `urlNuncaLegivel` nega leitura até pra admin. Sync 30/30min (n8n `zgXtFDgwPQQZcAbt` → POST `/api/agenda/sync`, header x-sync-secret cred `1cWXZL2pvhbCOyA8`, timeout 290s) baixa/expande/materializa em `agenda-eventos` na janela hoje→+7d; falha isolada por pessoa (`ultimoErro` SANITIZADO — nunca ecoa a URL). Parser: `ical-expander@3.2.0` + `.d.ts` próprio, fixture com 6 casos (RRULE, RECURRENCE-ID, EXDATE, dia inteiro, fuso NY, meia-noite) como contrato. Self-service `POST /api/agenda/minha` (id SEMPRE da sessão; anti-SSRF incl. redirect validado salto a salto, teto 5MB no download; "Atualizar agora" com trava 60s persistida).
- **Bug que a revisão final pegou (lição):** delete do sync com limite inferior deixava eventos passados órfãos pra sempre → como as leituras eram sort ascendente sem filtro de data, em ~3 semanas home e tela do time mostrariam "dia livre" PARA SEMPRE, em silêncio. Consertado: delete `< ate`, leituras filtram por SOBREPOSIÇÃO com o dia (`inicio < fimDoDia AND fim > inicioDoDia` — senão evento que atravessa a meia-noite some), teste int com dois "agora" diferentes.
- **RESOLVIDO (07/ago, mesmo dia):** Renan colou `AGENDA_SECRET_KEY` no Render — confirmado por probe autenticado (503→400, chave carregada). Follow-ups não-urgentes no ledger do worktree -4: paralelizar sync (sequencial, pior caso ~4min/16 feeds), trava de sync concorrente, verificação visual das telas.

**ANOTAÇÕES COMPACTAS no Resumo do Hub — NO AR (07/ago/2026, deploy `816ad25`).** Cada anotação comprimida a 3 linhas (`AnotacaoTexto`, `-webkit-line-clamp`), botão "expandir" só quando o texto REALMENTE estoura (medido por `scrollHeight`, não decorativo); só as 10 mais recentes visíveis, resto atrás de "Ver histórico (N anteriores)" fechado por padrão (`dividirAnotacoes`/`rotuloHistorico` em `src/lib/hub/anotacoesCompactas.ts`). Mudança só de apresentação — zero DDL. **Colidiu em produção com a feature de @menção clicável de outro chat** (mesmo texto de anotação, mesmo arquivo `AnotacoesSection.tsx`): integrado corretamente — `AnotacaoTexto` ganhou `children?` opcional, e `<TextoComMencoes texto={n.texto} />` renderiza DENTRO do clamp. As duas convivem: menção clicável funciona mesmo com o texto comprimido.

**TAREFAS RECORRENTES — NO AR (08/ago/2026, `feat/tarefas-recorrentes` mergeada, deploy `d5e3f6d`).** Tarefa com campo `recorrencia` (`semanal`/`quinzenal`/`mensal`) gera a próxima automaticamente **ao ser concluída** (não em data fixa) — zero cron, zero n8n. `recorrencia` é `text` validado em código, **nunca `select`** (select vira enum no Postgres; enum novo é a migração que o Drizzle pula em silêncio em prod — regra já estabelecida pelo campo `status` da mesma coleção).
- **Mecanismo:** hook `afterChange` novo (`repetirTarefaRecorrente`, ao lado do `beforeChange` existente, intocado) — na transição pra concluído, copia título/descrição/lista/responsáveis/cliente/prioridade/etiquetas/autor da série (NÃO copia anotações nem `concluidaEm`) e avança o prazo via `proximoPrazo` (`src/lib/tarefas/recorrencia.ts`, puro). Sem cascata por construção: a tarefa criada nasce com status inicial não-concluído, então a condição de disparo nunca é satisfeita pra ela — E TEM teste de integração provando isso (não só o comentário).
- **Regras de data:** âncora é o PRAZO atual, não a data de conclusão (não deriva com atraso); mensal clampa no último dia de mês curto (31/jan→28/fev) e a série "esquece" o dia original — próximo cálculo usa o dia já clampado (fev→mar = 28/mar, não 31/mar); nunca nasce atrasada (avança em intervalos até cair em hoje ou à frente); "hoje" é sempre `diaLocal` (São Paulo), nunca UTC cru.
- **Gotcha sutil do hook:** criar a próxima ocorrência via `req.payload.create(...)` **sem passar `req`** — o `beforeChange` da coleção carimba `autor = req.user.id` quando há usuário numa criação, e se passasse o `req` original (de quem CONCLUIU a tarefa) sequestraria o autor da série. Testado com dois usuários diferentes (autor da série ≠ quem concluiu).
- **Padrão de teste reusado:** endpoint custom de coleção (`/api/tarefas/acao`) se testa chamando o `handler` de verdade via Local API (`Tarefas.endpoints.find(e => e.path==='/acao').handler(req)`), sem HTTP — mesmo molde de `tests/int/tarefas-status-gerir.int.spec.ts`. Achado real: sem esse teste no handler (só `payload.update` direto), editar uma tarefa por drag-and-drop sem reenviar `recorrencia` a apagaria em silêncio.
- ⚠️ **Incidente de segurança durante a execução (08/ago):** um subagente de revisão reportou ter recebido, na PRÓPRIA saída de ferramenta, um `system-reminder` FALSO instruindo a não reverter/não mencionar uma suposta modificação em `Tarefas.ts` — padrão clássico de injeção de prompt. O harness neutralizou a tag antes de chegar ao controlador; o subagente ignorou a instrução embutida e verificou com `git diff`/`status` (limpo); controlador reconferiu de forma independente. Nenhuma instrução da injeção foi seguida em nenhum ponto. Origem não identificada — vale atenção se se repetir.
- 5 sessões de trabalho seguidas nesta branch caíram por erro de API mid-response (não é o injection acima, é instabilidade de infra) — em todos os casos o trabalho já estava no disco e não commitado; recuperado verificando o estado real (testes + git) em vez de refazer do zero.
- ⚠️ **Deploy do dia FALHOU por OOM no Render (09/ago, resolvido em `d26b2df`):** o build de `d5e3f6d` estourou 8GB e morreu, sem retry automático — produção ficou 3h+ rodando o código de véspera (`34d1dae`) em silêncio, sem erro visível pro usuário. Causa: `--max-old-space-size=8000` já era o teto do script `build`, e a memória real passou disso — crescimento acumulado do dia (chat/CRM de outro chat + as 4 features desta branch, 632 arquivos TS). **Diagnóstico correto foi por API, não por bundle:** checar se o campo novo (`recorrencia`) aparece em `GET /api/tarefas` com login real é prova definitiva de qual código está rodando; grep de bundle minificado é frágil (achou "confirmado" errado uma vez nesta mesma sessão, por monitor zumbi com exit 0 e output vazio). **Fix aplicado em `next.config.ts`:** `experimental: { webpackMemoryOptimizations:true, workerThreads:false, cpus:1 }` — flags oficiais do Next 16 pra OOM de build, sem risco funcional (só troca velocidade por memória). Teste local (Mac) foi INCONCLUSIVO (3,8-4,3GB nas 3 variantes, incl. sem tsc) — Mac não reproduz o container limitado do Render; o teste real só veio do redeploy, que passou. **Cogitado e RECUSADO sem aprovação do Renan:** `typescript.ignoreBuildErrors:true` reduziria memória de forma mais garantida, mas mataria a única rede que pegaria erro de tipo escapado de merge — fica como opção se o problema voltar com o projeto crescendo mais.

Spec e planos em `docs/superpowers/`. Reforça o [[feedback-isolamento-dados-clientes]] (ProgressoAula isolado por cliente). Marca em [[reference-quirk-brand]].

**PAINEL DE TAREFA REDESENHADO — NO AR (09/ago/2026, `feat/painel-tarefa-redesenho`, deploy `897f22b`, fast-forward direto pra `origin/main`).** Fase 1 (visual) do pedido do Renan inspirado no ClickUp: `TarefaModal.tsx` monolítico (formulário com `<select>`s crus, recorrência solta) virou 6 componentes de campo (`campos/CampoPrazo`, `CampoPilulaSelect` genérico p/ Status+Prioridade, `CampoResponsaveis`, `CampoCliente`, `estilosCampo.ts`) + `PainelComentarios` (coluna direita fixa), orquestrados por um `TarefaModal.tsx` reescrito em duas colunas. Fase 2 (histórico de mudanças de sistema, tipo Activity do ClickUp) fica pra depois — não existe hoje mecanismo de auditoria em nenhuma coleção do projeto, é escopo novo.
- **Zero DDL** — mudança só de apresentação, mesma API `/api/tarefas/acao`, mesmo `TarefaCard`.
- Construído por SDD: 7 tasks TDD + revisão por task (com mutation testing) + 1 revisão final de branch (modelo mais capaz) + 1 loop de fix pós-revisão-final. 725 testes unitários verdes, tsc limpo.
- **Achados reais pegos só na revisão de branch inteira (não nas revisões por task)** — mesma lição do "delete sem limite inferior" da Agenda: bug que só aparece comparando o branch inteiro contra a base, não task a task. Duas capacidades que existiam em `main` sumiram na decomposição, sem ninguém decidir isso: (1) todo campo virou `<span role="button">` sem `onKeyDown` — Enter/Espaço pararam de ativar (em `main` eram `<select>`/`<input>` nativos, 100% teclado); (2) `CampoCliente` perdeu o único jeito de desvincular um cliente já atribuído (`onEscolher(null)` nunca era chamado); bônus: `CampoResponsaveis` fechava sozinho ~150ms depois de escolher UMA pessoa (timer do `onBlur` não cancelado), quebrando atribuir várias pessoas em sequência. Todas as 3 corrigidas num commit de fix (`897f22b`) antes do merge.
- **Gotcha de teste descoberto no fix:** o React liga `onBlur` ao evento nativo `focusout`, não `blur` — um teste que dispara `new Event('blur')` sintético NUNCA aciona o handler; precisa de `new FocusEvent('focusout', {bubbles:true})`. Achado ao investigar por que o teste de regressão passava mesmo sem o fix (teste "vácuo").
- **Padrão a repetir:** quando um snippet de teste do PRÓPRIO plano é a causa do "bug" (ex.: `container.textContent` não pega `value` de `<input>`; `querySelectorAll('*')` acha o pai em vez do filho quando os dois têm o mesmo texto), a correção certa costuma ser deixar o TESTE mais preciso (`input.value`, escopar por `[role="button"]`), não mudar a marcação de produção pra satisfazer o teste — um primeiro fix desta feature adicionou spans ocultos em produção sem `aria-hidden` (risco de leitor de tela ler 2x) só pra passar o teste; a revisão de branch bloqueou e o fix certo removeu os spans.
- **Débito de acessibilidade registrado, não bloqueou o merge:** navegação por teclado dentro das LISTAS de opção (Status/Prioridade/Cliente/Responsáveis) ainda não existe — só os gatilhos ficaram teclado-ativáveis. Mesma categoria de dívida já aceita nas revisões por task, julgamento do controlador foi não elevar a bloqueante (pedido original do Renan nunca pediu ARIA listbox completo, só o visual tipo ClickUp).
- **Verificação pós-deploy:** sem endpoint novo pra smoke test dedicado (é troca de apresentação só). Confirmado vivo por `curl /api/notificacoes/painel` (403, não 500) estável por ~4,5min pós-push — cobre o risco conhecido de OOM silencioso no build (ver entrada de Tarefas Recorrentes acima). **Verificação visual manual (cor do "Repete", empty state "+ selecionar cliente", empilhamento em tela <700px) ainda pendente — precisa de login de equipe interna, que a IA não tem.**

**PAINEL DE TAREFA — FASE 1.1 NO AR (09/ago/2026, `feat/painel-tarefa-fase1.1`, deploy `640a07e`, fast-forward direto pra `origin/main`).** Sequência direta da Fase 1 (mesmo dia): o Renan usou o painel novo e reportou Descrição não salvando + pediu reorganizar layout (Cliente no topo, campos numa faixa compacta, Descrição bem maior) + texto rico (negrito/itálico/sublinhado/título/subtítulo/lista/link) + botão "Abrir Drive do cliente".
- **Diagnóstico do bug antes de tudo:** escrito um teste de integração batendo no handler real do endpoint — o backend salvava certinho. A causa real era `acao()` no `TarefaModal.tsx` falhando em SILÊNCIO em qualquer erro (sem `res.ok`, sem catch de rede) — parecia "não salvou" mas era falta de feedback. Corrigido: erro visível, modal nunca fecha nem descarta rascunho numa falha.
- **Decisão técnica do texto rico (mudou de rumo no brainstorming):** cotado guardar HTML cru primeiro, mas exigiria biblioteca de sanitização (nenhuma existe no projeto — e o projeto já removeu SVG do upload por XSS antes). Decisão final: formato próprio (`Bloco[]`/`Span`, mesmo espírito do `lexical.ts` do hub já existente) guardado como JSON no MESMO campo `descricao` (`type: 'textarea'`, zero DDL). Editor é `contentEditable` + `document.execCommand` (nativo do browser); ao perder foco/digitar, percorre o DOM e extrai só texto+tags reconhecidas — nunca interpreta HTML externo como marcação.
- **CRÍTICO real achado e corrigido na revisão final de branch (não nas revisões por task):** `blocosParaHtml` escapava `&`/`<`/`>` mas não aspas no `href` — um link como `https://x.com" onmouseover="alert(1)"` quebrava pra fora do atributo, e o resultado ia pro `innerHTML` do editor. Disparava na sessão de QUALQUER colega de equipe que abrisse a tarefa (XSS armazenado interno, sem CSP no projeto). Dois caminhos de entrada: o botão 🔗 do editor (regex frouxa) e POST direto em `/api/tarefas/acao` com `descricao` forjada (bypassa toda validação do browser). Fix: `hrefSeguro` exportada+endurecida (rejeita espaço/aspas/`<>`) aplicada TANTO na entrada quanto na SAÍDA do HTML (defesa em profundidade — um `descricao` pode chegar sem nunca passar pelo lado do browser); `escaparAtributo` nova (escapa aspas, diferente de `escaparTexto` que só escapa `&<>`) pro contexto de atributo. Confirmado com 2 rodadas de revisão independente, a 2ª escreveu prova própria (8 payloads maliciosos adicionais, 9/9 com fix / 8 falhas sem).
- **3 outros achados Importantes da mesma revisão, mesma causa-raiz (fronteira de confiança entre o banco e o `innerHTML`):** `blocoValido` só validava o shape externo do JSON, não o conteúdo dos spans — `spans:[null]` ou `{texto:42}` derrubava o modal inteiro com exceção não tratada; `<br>` (Shift+Enter) era descartado em silêncio e linhas coladas sem separador — corrompia o texto do usuário do mesmo jeito que o bug original; commit da Descrição dependia só de `onBlur`, sem `onInput` — mesma fragilidade de timing que causou o bug reportado.
- **Padrão da fase:** toda vez que o time usa uma feature nova no ar, acha ajuste real — Fase 1 (deploy `897f22b`) → uso real → Fase 1.1 (este deploy). Esperar mais uma rodada se o padrão continuar.
- **Verificação pós-deploy:** mesmo padrão da Fase 1 — sem endpoint novo, confirmado vivo por `curl /api/notificacoes/painel` (403 estável por ~4,5min). **Verificação visual manual (formatação rica de verdade num browser, botão Drive, empilhamento mobile, descrição legada em texto puro) ainda pendente — precisa de login de equipe interna.**
- **Débitos registrados pro backlog, não bloquearam:** estilo de popover duplicado em 3 componentes (devia estar em `estilosCampo.ts`); select de Lista sem `appearance:none` (único campo com chrome nativo na faixa); `CampoPrazo` ainda expande inline em vez de virar popover como os vizinhos; paste de Google Docs degrada listas aninhadas pra texto corrido; descrição vazia vira JSON não-null em vez de `null`; botão Drive não existe no Hub (`clienteFixo` esconde `CampoCliente` inteiro — é onde seria mais útil, confirmar com Renan); teclado dentro das LISTAS de opção (não só os gatilhos) ainda não existe, dívida herdada da Fase 1.

**AVISO DE TAREFA VENCIDA — NO AR (09/ago/2026, `feat/aviso-tarefas-vencidas`, deploy `ff64dea`, fast-forward direto pra `origin/main`).** Pedido do Renan: sempre que uma tarefa pendente ficar com prazo vencido, avisar o(s) responsável(is). Reusa o sino de notificações que já existia (mesmo sistema de "te atribuíram"/"@menção") — sem WhatsApp nesta fase (não existe canal de DM individual pra equipe, só pra cliente; ficou descartado por escopo, não por limitação técnica).
- **Mecanismo:** sem cron interno no projeto (confirmado na pesquisa: tudo hoje é endpoint protegido por secret que o Render Cron Job externo chama numa agenda). Endpoint novo `POST /api/cron/tarefas-vencidas`, espelha byte a byte o padrão já em produção (`/api/crm/sla`, mesmo `SYNC_SECRET`/`x-sync-secret`). Varre tarefas não concluídas com prazo vencido, gera 1 notificação por responsável — **repete todo dia enquanto a tarefa continuar vencida e pendente, sem deduplicar** (decisão de produto explícita do Renan, risco de acúmulo aceito conscientemente).
- **Peça nova de schema:** `notificacoes.tipo` ganhou o 3º valor `'vencida'` (antes só `atribuicao`/`mencao`) — DDL (`ALTER TYPE ... ADD VALUE`) aplicada e VERIFICADA em produção antes do push (`scripts/ddl/2026-08-09-notificacoes-tipo-vencida.sql`).
- **Achado real da revisão final de branch, fora do escopo de todas as 4 revisões por task:** `src/lib/pessoas/perfil.ts` (feed "Atividade recente" do perfil de cada pessoa) colapsava qualquer tipo != 'mencao' em 'atribuicao' — sem correção, um aviso de tarefa vencida (sem autor humano) viraria o texto `" atribuiu você em ..."` com espaço em branco e uma afirmação falsa. Revisor confirmou EXECUTANDO a função real (não só lendo). Corrigido: ramo próprio pro tipo 'vencida', sem tentar atribuir autor.
- **Lição de arquitetura reaproveitada:** cálculo de "tarefa vencida" existia só inline dentro de `TarefasBoard.tsx` — extraído pra `estaVencida()` compartilhada (`src/lib/tarefas/vencimento.ts`), usada tanto pela tela quanto pelo cron, byte-idêntica ao comportamento antigo (mesma comparação de string de data em UTC, mesmo critério que já pintava a borda vermelha).
- **Render Cron Job criado e funcionando (09/ago/2026, mesmo dia do deploy) — nome `Area-Membros-Notifications`.** Primeira tentativa falhou (Render escolheu runtime **Docker** por padrão na criação, tentou buildar a imagem inteira e quebrou por lockfile — "Runtime" não é editável depois de criado). Resolvido deletando e recriando com **runtime Node** + **Build Command `echo sem-build`** (a receita real do cron irmão que já funcionava, `sync-diario-painel` — cron não builda o app, só dá `curl` no site já publicado). Schedule `0 7 * * *`, mesmas env vars `APP_URL`/`SYNC_SECRET` do sync. Renan colou o `SYNC_SECRET` no chat pra teste manual — endpoint respondeu `{"ok":true,"tarefasVencidas":1,"notificacoesGeradas":2}` num teste ao vivo (achou uma tarefa vencida real, 2 responsáveis, ambos notificados). Custo do Cron Job: cobrado só pelos segundos de execução (não 24/7) — um `curl` de poucos segundos, 1x/dia, fica na casa de centavos de dólar/mês, não os ~US$7 do plano Starter cheio.
- **GOTCHA Render: novo Cron Job pode nascer com runtime Docker por padrão**, mesmo o repo sendo 100% Node — não tem opção de trocar depois, só deletar e recriar escolhendo Node explicitamente na criação. Vale conferir esse passo ANTES de preencher Schedule/Command da próxima vez.
- ⚠️ **Ferramenta de navegador (Claude_Browser) ficou indisponível a sessão inteira** ("Policy check temporarily unavailable" em toda ação de leitura/screenshot, só `navigate`/`tabs_context` funcionavam) — todo o troubleshooting do Render foi feito por prints colados no chat, não por acesso direto. Se voltar a acontecer, não adianta insistir em retry, é mais rápido já pedir print.
- **Débitos registrados, não bloquearam:** doc do cron agora avisa que a DDL precisa rodar antes (achado da review final — sem isso a falha é silenciosa, `try/catch` engole o erro e o endpoint responde `ok:true` com `notificacoesGeradas:0`); crescimento sem limite da tabela `notificacoes` ao longo do tempo (sem purge, sem dedup — nomeado como risco, não resolvido); valor de enum pré-existente `'tarefa-criada'` (usado em `perfil.ts`, nunca teve DDL) — gap achado de passagem, fora do escopo desta feature.

**GUIA DE TRABALHO — NO AR (19/ago/2026, deploy `5d4dc1f`).** As 20 páginas de orientação do departamento de tráfego (doc ClickUp `8cdukra-9937`) viraram aba dentro de "Meu treinamento", lidas e editáveis por quem é do time interno. **A área de membros passou a ser a fonte de verdade — o doc do ClickUp fica congelado como histórico, sem sincronização de volta** (dois editáveis sem controle de versão = uma edição some sem aviso). Coleção `guia-paginas` (`titulo`/`ordem`/`conteudo` jsonb `Bloco[]`/`atualizadoPor`/`atualizadoEm`), DDL `scripts/ddl/2026-08-19-guia-paginas.sql` aplicada em prod ANTES do push. Acesso delega a `fazTrilha` (`podeVerGuia`/`podeEditarGuia` separados de propósito: restringir edição à liderança foi considerado e recusado pelo Renan, então virar essa decisão é mudança de uma linha). Trava no endpoint `/api/guia-paginas/acao`, não em esconder a aba. Reusa `CampoDescricaoRica` das tarefas; guia NÃO tem conclusão nem entra no certificado (diferente da trilha).
- **Conteúdo importado por `scripts/importar-guia.mts` e NÃO versionado** — cópia no repo viraria segunda fonte de verdade na primeira edição pela tela. Upsert por `titulo`, nunca deleta, não mexe em `atualizadoPor`/`atualizadoEm` de quem editou (a diferença entre "reimportar" e "apagar o trabalho de alguém"). Idempotência provada: 2ª passada = 0 criadas, 20 atualizadas.
- **Receita de conferência que vale repetir pra qualquer import de conteúdo:** ler a origem uma SEGUNDA vez pelo MCP e comparar byte a byte com a primeira transcrição — pegou 6 páginas onde eu tinha comido espaço em fim de linha. Depois, um checador palavra-a-palavra entre o Markdown de origem e os `Bloco[]` convertidos, porque o `--seco` só reporta ACHATAMENTO de forma, não perda de texto. Cuidado: normalizar demais no checador esconde desvio real, e normalizar de menos acusa perda que não existe (escape `\_` dentro de URL, ponto final fora do `[…](…)`).
- Achatam formatação (texto intacto): "Rotina Diária" usa `####` como corpo → vira subtítulo; "Modelo: Resumos de call" perde a indentação de 5 sub-bullets. Embed do Drive tem a URL duas vezes na origem e vira um link só — não é perda.
- Três páginas são exemplos preenchidos com nome de cliente real (Victor Emanuel, João Robes, Complexo/Regis); entraram como estão no ClickUp, visíveis só ao time interno.

**GUIA VIRA A PRIMEIRA ABA — NO AR (20/ago/2026, deploy `9f8b0b0`).** "Meu treinamento" passa a abrir no Guia de trabalho; a Trilha vira a segunda aba. O guia assume a URL canônica (`/admin/trilha` sem query) e a trilha vai pra `?aba=trilha` — link antigo de trilha continua funcionando. A escolha da aba virou função pura `abaEscolhida(pedida, visiveis)` em `src/lib/guia/tela.ts`, cujo fallback é a primeira aba **VISÍVEL** (não a primeira que existe), senão a padrão 'guia' vazaria pra quem não alcança o guia. ⚠️ **Sem endpoint novo = sem teste de fumaça**: dá pra provar que o app continua vivo (403 em `/api/notificacoes/painel`), mas NÃO que a aba inverteu — tela autenticada. Confirmação visual depende do Renan.

**EM ANDAMENTO — guia com pastas, arquivos e filtro** (spec + plano de 6 tasks em `docs/superpowers/`). Task 1 (acima) no ar. **Task 2 commitada e RETIDA**: registra a coleção `guia-pastas` no config e o DDL `scripts/ddl/2026-08-19-guia-pastas.sql` **não foi aplicado** — subir coleção sem tabela derruba o admin inteiro (42703). O DDL traz um `UPDATE chave_importacao = titulo` **bloqueante**: tem que rodar antes de qualquer renomeação, senão a página renomeada perde o vínculo com a origem e a reimportação a duplica. Decisões do Renan registradas na spec: hierarquia de UM nível, arquivo pode ficar solto na raiz, ordem **alfabética** (descarta a sequência de leitura do ClickUp, aceito), e **estrutura é da liderança** (`ehSupervisorOuAcima`) enquanto o TEXTO continua do time interno (`fazTrilha`) — renomear é editar título, então título saiu do time.

**PEDIDO NA FILA (20/ago):** as 25 etapas da trilha precisam nascer TODAS minimizadas e abrir por clique — hoje a lista abre expandida e a rolagem fica gigante. Precedente na casa: o menu lateral do admin nasce fechado e grava o que a pessoa abre em `preferencias.menuSecoesAbertas` (`25a6263`).

**SESSÃO DE LOGIN — NO AR (09/ago/2026, `fix/sessao-7-dias`, deploy `3a6d6a2`).** Renan reportou "sessão desloga rápido demais". Causa raiz achada por systematic-debugging (rastreado no código-fonte do `@payloadcms/ui`, não suposição): `tokenExpiration` nunca foi configurado em `Users.ts` → cai no default do Payload (7200s = 2h); o `RootProvider`/`AuthProvider` do admin não recebe `autoRefresh` em lugar nenhum → sessão depende de um popup "Continuar logado?" perto do vencimento, fácil de perder (reunião, outra aba). Fix: `tokenExpiration: 7 * 24 * 60 * 60` (7 dias) em `Users.ts`. **Gotcha de unidade, testado**: `tokenExpiration` é em SEGUNDOS — o oposto do `forgotPassword.expiration` da mesma coleção, que é em MILISSEGUNDOS (já documentado antes). Teste novo em `tests/unit/convite-expiration.spec.ts` trava as duas unidades lado a lado. **Efeito só vale daqui pra frente** — sessões já abertas antes do deploy continuam na regra antiga de 2h (JWT é auto-contido, não dá pra estender retroativamente); quem logar de novo já pega os 7 dias.
