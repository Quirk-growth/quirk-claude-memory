---
name: project_crm_quirk
description: CRM multi-tenant dentro da área de membros (Bolten+Imobilead); v1 no ar ago/2026; hierarquia dono/gerente/vendedor; pendências UAZAPI
metadata: 
  node_type: memory
  type: project
  originSessionId: 74e3c39b-64af-48ce-9da1-ebcfb16c3a2b
  modified: 2026-08-06T09:54:50.234Z
---

CRM próprio da Quirk dentro da [[project_area_membros_quirk]], pros CLIENTES usarem (multi-tenant). Une Bolten (contato WhatsApp vira lead, kanban editável) + Imobilead (filas por campanha, roleta de vendedores). Spec `docs/superpowers/specs/2026-08-02-crm-quirk-design.md`; 3 planos em docs/superpowers/plans/2026-08-02-crm-*.md. **v1 deployada em 02/ago/2026** (21 commits, 85a1c33..82a32bf) via subagent-driven development (16 tasks, review por task + review final).

**Arquitetura v1 (fase A — sem chat):** 4 coleções `crm-leads/vendedores/filas/conexoes` + `clientes.crmAtivo/crmEtapas/crmWebhookToken` + `users.papelCliente` (dono/gerente/vendedor — campo travado isAdminField, convites via /convidar-time). UAZAPI segura as conexões WhatsApp (QR); nosso lado só recebe webhooks — NADA de mensagens armazenadas. Fases futuras: B = chat leitura sob demanda da UAZAPI; C = chat completo (serviço separado).

**Regras-chave:** dedupe por (cliente, telefone normalizado DDI55) — UNIQUE no banco; roleta round-robin com ponteiro persistido na fila (fila vazia → round-robin geral entre ativos); vendedor só vê/edita os leads dele (bypass null corrigido); webhook genérico `/api/crm-leads/entrada/<token>` (Meta/Google/TikTok via Make/n8n); webhook UAZAPI `/api/crm/whatsapp?token=`; página `/crm` (dark) + aba CRM no hub (tema escopado `.hub-crm-tema`); ativação = switch no Cadastro (supervisor+) que gera etapas padrão + token + fila Geral.

**Evoluções pós-v1 (02-03/ago):** Visão geral executiva como entrada (KPIs+funil barras+vendedor+campanhas); ficha do lead em modal grande com respostas de formulário (`crm_leads.respostas` jsonb — webhook aceita `respostas:[{pergunta,resposta}]`); sub-menu do CRM na barra lateral (?secao=); **CRM→Meus resultados**: Ganhou cria venda (vendas.leadCrm, ticket acompanha valor), marcos por etapa (`marco` atendimento/agendamento → carimbos atendidoEm/agendadoEm → funil soma manual+CRM via funilCrmPorMes); Vendedores = só gestão (entrada pelo Convidar time); **SLA da roleta** por fila (5/10/15/30min, transferirLeadsVencidos: transfere pro próximo da fila, varre no tráfego + POST /api/crm/sla com x-sync-secret pra n8n 5min). **UAZAPI endpoints corretos (uazapiGO v2)**: POST /instance/create (admintoken), POST /instance/connect (token, body {} → QR), GET /instance/status, POST /webhook modo simples com excludeMessages wasSentByApi **+ `enabled: true` OBRIGATÓRIO (registra desabilitado por default — bug real que segurou os primeiros leads, 03/ago)**. Base do Renan: https://quirkgrowth.uazapi.com; token da instância fica em crm_conexoes (dá pra diagnosticar via GET /webhook e POST /message/find com ele). Payload real da mensagem VALIDADO (03/ago): {chatid, sender, senderName, fromMe, isGroup, text, content...} — mapeador compatível. Status fresco + polling pós-QR + auto-cura de webhook (webhookConfiguradoEm) implementados.

**Etapas de SAÍDA + ganho oficial (03/ago, f0e6419):** `EtapaCrm.saida` (DDL clientes_crm_etapas.saida) — etapa de saída NÃO vira coluna do kanban: vira chip no trilho droppable "Saídas do funil" (clique abre a lista filtrada); visão geral mostra chips separados e exclui esses leads de em-negociação/parados. No comercial: retorno_futuro, arquivado, acomp_futuro, arquivados_pos_call e clientes são saídas; `contratos_do_mes` renomeado pro slug `ganhou` (nome "Contratos do mês") → alimenta KPIs de ganho/conversão + venda automática; coluna Clientes saiu do fluxo (convertido continua indo pra ela como saída). Seed aplicado em prod no cliente 104. Menu Pessoas: "Gestores"→"Time Quirk", labels users Pessoa/Time Quirk. Foto de perfil: FotoPerfilField (upload direto, trocar/remover) via POST /api/users/foto-upload (cria media com overrideAccess — gestor não tem create em media; só grava o arquivo, o campo salva no form).

**Área Comercial da Quirk (03/ago, 35de4fe):** pipeline interno = cliente interno slug `quirk-comercial` (id 104, flag clientes.interno esconde de listas/BI/financeiro), 22 etapas espelhadas do ClickUp (lista prospectos). Papel `comercial` (só vê o departamento). Lead com descricao/etiquetas/dataAgendamento/meetingUrl; webhook SAÍDA agenda = env `COMERCIAL_AGENDA_WEBHOOK_URL` (POST ao mover pra reuniao_agendada com data); converter→cliente hub (/converter, criarClienteSemLogin). Views /admin/comercial (dashboard) + /admin/comercial-funil. Entrada de leads: /api/crm-leads/entrada/<token do cliente 104>. Fotos: users.foto + AvatarPessoa nos comentários (CRM/rotina/hub); remover número WhatsApp na config.

**Conexão nativa Meta Lead Ads (03/ago, 021342b, NO AR):** webhook leadgen em tempo real via app "BM - Clientes Quirk" (id 1473020417267373) + token de sistema QuirkPainel (páginas dos clientes são ativos da BM 1612905538806887 → sem OAuth/review; acesso padrão basta). Peças: src/lib/crm/meta.ts (listarPaginas owned+client_pages, listarForms via page token, inscreverPagina subscribed_apps leadgen, buscarLead/mapearLeadMeta field_data→lead+respostas), rotas /api/crm/meta/{paginas,forms,conectar} (SÓ equipe — isolamento), receptor /api/crm/meta-webhook (GET hub.challenge, POST assinado sha256 META_APP_SECRET; sempre 200), config em clientes.metaLeadForms (jsonb), seção "Meta Lead Ads" no CrmConfig, origem 'meta' (enum alterado em prod). SETUP META (03/ago): app usa painel NOVO "Casos de uso" (sem Análise do App) — adicionei o caso "Capturar e gerenciar leads de anúncios" + permissão pages_manage_metadata dentro dele (tudo "Pronto para teste" = standard access, sem review). **As páginas utilizáveis vêm de /me/accounts (atribuídas ao QuirkPainel — 18 de 134 em 03/ago)**, NÃO de client_pages da BM; a BM das páginas é a **803799600742642 (Quirk Growth)** — a 1612905538806887 da memória do Auto Ads NÃO tem as páginas. Página nova → atribuir ao QuirkPainel em Config. do Negócio → Contas → Páginas. Erros diagnosticados: sem atribuição = #10 no access_token da página; sem pages_manage_ads = #200 no leadgen_forms. FEITO (03/ago): webhook do app configurado (callback /api/crm/meta-webhook + verify token + campo leadgen ASSINADO no produto Page); envs META_WEBHOOK_VERIFY_TOKEN (=47eff3d...) e META_APP_SECRET validadas em prod (verify→200; assinatura errada→403). Dá pra checar o token REAL do Render sem expô-lo pelo Web Shell do Render (node -e lendo process.env.META_ACCESS_TOKEN → /me/permissions). PENDENTE (Renan): o token no Render AINDA está sem pages_manage_ads (lista forms) + pages_manage_metadata (inscreve página no leadgen) — só tem leads_retrieval/pages_show_list/pages_read_engagement. Regenerar token do QuirkPainel MARCANDO essas 2 (já visíveis no app após adicionar o caso "Capturar leads") + colar no Render. Depois: teste e2e (buscar página funciona só com pages_show_list; listar forms e salvar conexão exigem as 2 que faltam).

**TESTE E2E (03/ago):** token novo validado (8 escopos OK, listou forms de página real). Conectei o form "[Quirk] Formulário" (id 1593172628528609) da página Quirk Growth (829710846881192) ao cliente 69 (#CA - Vitacon Quirk - Pix) via script: subscribe leadgen = success (prova pages_manage_metadata). Disparei lead de teste pela Ferramenta de Teste de Anúncios de Lead (developers.facebook.com/tools/lead-ads-testing → página + form + Criar lead). RESULTADO: nosso pipeline 100% OK, mas o lead NÃO entregou — "Acompanhar status" mostrou **Failure: CRM access has been revoked from Lead Access Manager** pros DOIS apps (o nosso 1473020417267373 e o Apresenta.me). ⚠️ BLOQUEIO FINAL = cada PÁGINA precisa autorizar nosso app no **Gerenciador de Acesso a Leads** (Lead Access Manager) — Configurações da Página / Business Settings → Integrações → Acesso a leads → adicionar o CRM/app às páginas. É config de permissão do lado do Meta (não código). Sem isso o webhook nunca dispara. Depois de autorizar: re-disparar (excluir o lead de teste antes — 1 lead por form) e conferir na Vitacon.

**RESOLVIDO + PROVADO (03/ago):** Renan autorizou o acesso a leads na BM **"Quirk - Growth Marketing" (id 803799600742642 — é a BM dona do app e das páginas; NÃO a 1612905538806887 do Auto Ads)**. Re-disparei o lead: Meta entregou **Success** (payload real com leadgen_id/form_id/page_id). Log do Render provou pipeline 100%: `meta-webhook: lead <id> → cliente 69 (invalido)`. O (invalido) é só porque a Ferramenta de Teste preenche phone com o texto `<test lead: dummy data for phone_number>` e o motor rejeita telefone inválido (chave de dedupe). Com lead REAL (telefone válido) cai no funil. Integração Meta Lead Ads FUNCIONANDO. Pendências: (a) pra cada cliente novo, atribuir a página ao QuirkPainel + conectar o form no CRM + autorizar no Lead Access Manager; (b) o form de teste "[Quirk] Formulário" ficou conectado ao cliente 69 (Vitacon) — remover depois do teste pra Quirk não mandar lead real pra lá; (c) considerar aceitar lead só-com-email (hoje exige telefone válido).

**Escopo página↔cliente / isolamento do gestor (03/ago, cb791f7 — Plano B):** furo achado pelo Renan — o token QuirkPainel enxerga TODAS as páginas, então os endpoints /paginas e /forms (gate PAPEIS_EQUIPE incluía gestor) deixavam qualquer gestor enumerar páginas/forms de TODOS os clientes. Fix: `clientes.metaPaginas` (jsonb, páginas atribuídas a cada cliente) + /paginas restrito a supervisor+ (ehSupervisorOuAcima) + novo /atribuir-pagina (supervisor+ vincula/desvincula; remover derruba forms órfãos) + /forms e /conectar exigem podeGerenciarCliente **E** página∈metaPaginas do cliente. SecaoMeta em 2 níveis: supervisor+ ATRIBUI página (busca geral); equipe (papel==='equipe', inclui gestor no hub) LIGA formulários só das páginas atribuídas; cliente member só leitura. Regra p/ features Meta futuras: buscar/enumerar ativos globais = supervisor+; agir num cliente = podeGerenciarCliente + recurso atribuído ao cliente.

**Confirmado com cliente REAL (05/ago):** lead do form "Quirk - ZAFIRA | Condicional | 30/07" da página **Andrei Soluções Imobiliárias** não entrou no CRM do cliente Andrei, apesar do Hub 100% certo (form vinculado à fila). Diagnóstico fechado na Ferramenta de Teste (página Andrei + form ZAFIRA + Acompanhar status): app 1473020417267373 = **Failure** → expandindo o aviso: **"App does not have leads permission" → Add this app to Lead Access Manager** (+ "Missing 'pages_read_engagement' permission"). Nenhum log `meta-webhook` no Render em 7 dias (retenção do free) porque a entrega falha ANTES de sair do Meta. App confirmado **Publicado**; não era modo dev. **Lição:** a autorização de 03/ago cobriu só a BM/página Quirk Growth (803799600742642) — NÃO as páginas próprias dos clientes. **Item OBRIGATÓRIO de onboarding: cada página de cliente precisa adicionar o app "BM - Clientes Quirk" no Gerenciador de Acesso a Leads DA PÁGINA** (Meta Business Suite → Configurações → Acesso a leads → CRMs → adicionar; ou Config. da Página → Central de Anúncios de Cadastro). Renan é admin da página (diagnóstico verde), então consegue fazer. Sem isso, webhook nunca dispara.

**Visão geral: filtros + funil de conversão (06/ago, branch feat/crm-visao-filtros → main 039fe32, NO AR):** a aba inicial (`?secao=visao`) ganhou barra de filtros — período (presets incluindo HOJE + intervalo custom), corretor, fila, formulário — via query params (default 30d) que recalcula KPIs+funil+listas (só na `visao`; kanban/lista seguem sem filtro). Funil agora mostra nº na etapa **+ % de conversão cumulativa** (base = leads em-jogo+ganhos, `alcancaram_i`=estado atual com ordem≥i; SEM histórico de etapa — limitação documentada). KPIs de janela fixa re-rotulam "no período" quando há recorte (`recorteAtivo`). **Fundação nova:** cada lead NOVO grava `fila` (a roleta já calculava, só não persistia) + `formularioId`/`formularioNome` (Meta, antes descartados) → `montarWhereLeads` (puro, testado) filtra no banco. Helper de período próprio `src/lib/crm/periodoLeads.ts` (inclui HOJE — ≠ `relatorios/periodo.ts` que vai até ontem). **DDL de prod aplicado À MÃO** (via script node+pg no endpoint DIRETO sem -pooler, `ep-patient-hill…/neondb`): `crm_leads.fila_id` (FK→crm_filas ON DELETE SET NULL) + `formulario_id`/`formulario_nome` varchar + índices `crm_leads_fila_idx`/`crm_leads_formulario_id_idx`/`cliente_fila_idx` — nomes extraídos do banco de TESTE (que tinha via push). Backfill `src/scripts/backfillCrmFila.ts` (cursor por id, isola erro/lead, exige `CONFIRMAR_BACKFILL=1`, **rodar com `NODE_ENV=production` p/ push OFF**) estimou fila por campanha em 12 leads antigos. Feito via subagent-driven (7 tasks TDD + review por task + review final; specs/plano 2026-08-05-crm-filtros-funil-conversao). PENDENTE: verificação user-facing (login de member — do Renan); alinhar merge da branch antiga `feat/crm-filtros-funil-conversao` (tem meus commits CRM originais + docs cobrança-Asaas) pra não duplicar.

**Pendências pro rollout:** (1) Render: `UAZAPI_BASE_URL` + `UAZAPI_ADMIN_TOKEN` (sem eles só o Conectar falha); (2) **validar payload real do webhook UAZAPI** — capturar amostra da instância do Renan e ajustar `mapearEventoUazapi` (src/lib/crm/uazapi.ts, único ponto de ajuste; endpoints /instance/* também a confirmar); (3) ativar primeiro na conta de teste renandmreal; ao criar vendedores, MARCAR na fila Geral. Follow-ups aceitos: CSV prévia+colunas etapa/vendedor; filtros campanha/origem na lista; testes int dos webhooks; UI pra gestor trocar papelCliente (campo agora é admin-only).

**FASE A — CONVERSAS WHATSAPP NO CRM (07-08/ago/2026, → main 75fd430, NO AR).** Inbox estilo WhatsApp dentro do CRM (spec/plano `docs/superpowers/{specs,plans}/2026-08-07-crm-conversas-whatsapp*`; runbook DDL `docs/superpowers/migracoes/2026-08-07-crm-mensagens.md` — APLICADO em prod, incl. `payload_locked_documents_rels.crm_mensagens_id`). Arquitetura decidida: CRM FICA na área de membros; IA/follow-ups (Fases B/C) rodarão no n8n falando com o CRM por endpoint. **Coleção `crm-mensagens`** (unique cliente+waMessageId = dedupe de webhook; access equipe-only, operações reais pelos endpoints com `autorizarCrm` + trava lead↔cliente↔vendedor). **Webhook UAZAPI** agora TAMBÉM persiste mensagens (`mapearMensagemUazapi` + `gravarMensagemEntrada`; áudio baixado pro R2 via Media com cliente/restrito:false; fromMe/isGroup seguem ignorados). **Endpoints** `/api/crm-mensagens/{listar,thread,enviar,assumir}` — thread com cursor + marca lida; enviar texto JSON ou áudio MULTIPART com field `_payload` (Payload ignora campos soltos!); assumir seta `crm-leads.atendidoPor` (kill-switch da IA Fase B). **Campos novos em crm-leads:** atendidoPor, ultimaMensagemEm, previaTexto, previaTipo (prévia DENORMALIZADA — poll por cursor não relê status; envio força refresh da thread ao resolver). **UI `CrmConversas`** (client, tokens --pt-*, verde WhatsApp fixo na bolha de saída): lista+thread+composer com emoji grid próprio e MediaRecorder (webm→mp4 fallback), Composer com key por conversa + leadAtivoRef guard (anti-vazamento/anti-corrida), mobile 1 painel; montada em `/crm?secao=conversas` (todos os papéis, menu topo) e no Hub (details no HubAbaCrm). **Gotchas aprendidos:** file-type SNIFFA bytes e ignora mimetype declarado (whitelist da Media precisa de video/webm + audio/x-m4a pros MediaRecorder); mensagem entregue NUNCA pode virar 'falhou' (bookkeeping em try próprio); AbortSignal.timeout nos fetch UAZAPI. **PILOTO (primeiro cliente): validar payload real da UAZAPI (shape presumido, marcado no código), /send/media real, e nota de voz do Chrome (webm/opus vs ptt ogg/opus — pode precisar transcodificar); fast-follows: atendidoPorNome/fila/etapa-label no /listar.** Ledger completo em `.superpowers/sdd/progress.md` seção "CRM Conversas WhatsApp".
