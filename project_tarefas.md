---
name: project_tarefas
description: Módulo de Tarefas do time interno (Portal da Equipe) na área de membros; gerenciador estilo ClickUp. NO AR ago/2026: Parte 1+v2 (lista/filtros/etiquetas/status), Parte 2 (inbox+menções), Parte 3 (perfil da pessoa), Parte 4 (chat de DM 1:1)
metadata: 
  node_type: memory
  type: project
  originSessionId: 635d4787-0e22-45b2-b202-ef558aebae16
  modified: 2026-08-15T14:44:51.447Z
---

Gerenciador de tarefas do **time interno da Quirk** dentro da [[project_area_membros_quirk]] (cockpit/Portal da Equipe — nunca aparece pro cliente `member`). Inspirado na lista "Tarefas Tráfego" do ClickUp deles (list 901702083148, space Gestão de Projetos), mas com **cliente estruturado** em vez de `[CLIENTE]` no título. Spec/plano `docs/superpowers/specs|plans/2026-08-06-tarefas-parte1-*`.

> **STATUS (re-verificado 06/ago, à noite):** v2 **mergeada e NO AR** — `git ls-remote origin main` = `9821de3` (ponta da feat/tarefas-v2) e `/api/listas-tarefas/gerir` responde 403 em prod (endpoint só existe na v2). Um chat paralelo tinha registrado "nunca mergeada" olhando a branch `main` **LOCAL** desatualizada — deploys são via `git push origin HEAD:main`, que NÃO move a ref local; sempre conferir com `git ls-remote origin main` ou `git fetch` antes de concluir estado da main.

**Parte 1 (NO AR, 06/ago, branch feat/tarefas → main edea30b):** feito via subagent-driven (8 tasks TDD + review por task + review final MERGE).
- **Collections:** `listas-tarefas` (status configuráveis POR LISTA, estilo ClickUp: `{slug,nome,cor,ordem,tipo:aberto|andamento|concluido}`, `minRows:1`) + `tarefas` (titulo/descricao/lista/status/responsaveis[users hasMany]/autor/prazo/prioridade[maxima|media|normal]/cliente[opcional]/concluidaEm). Status é validado contra a lista no hook `beforeChange` (`normalizarStatusTarefa`); `concluidaEm` derivado do `tipo=concluido`; `autor` sempre o criador. Acesso = `ehEquipe` em tudo (helper novo `isEquipe`); delete tbm = equipe.
- **Endpoint** `POST /api/tarefas/acao` (criar/editar/mover/excluir), gate `ehEquipe` 403.
- **Camada** `src/lib/tarefas/{tipos,listas,status,consulta}.ts`: `LISTAS_SEED`, helpers puros, `montarWhereTarefas`+`listarTarefas` (busca por título em memória; TarefaCard).
- **Telas:** view admin `TarefasView` no menu Tráfego+Comercial (`/admin/tarefas`, lista agrupada por status + modal criar/editar, componente client `TarefasLista` reutilizável) + **aba "Tarefas" no Hub** (`ABAS_HUB` += 'tarefas', `HubAbaTarefas`, escopo por cliente via `listarTarefas({clienteId})`, modo `clienteFixo`).
- **Seeds:** listas **Tráfego** (A fazer·Demanda recebida·Em andamento·Concluído) e **Criativos** (Demanda recebida·Em produção·Revisão de copy·Copy revisada·Criativo finalizado·Criativo revisado·Criativo aprovado·Concluído). Script `src/scripts/seedListasTarefas.ts` (idempotente por nome, exige `CONFIRMAR_SEED=1`, rodar `NODE_ENV=production`).
- **DDL prod aplicado à mão** (extraído do banco de TESTE, endpoint direto sem -pooler): tabelas `listas_tarefas`, `listas_tarefas_statuses` (array), `tarefas`, `tarefas_rels` (responsaveis hasMany) + enums `enum_listas_tarefas_statuses_tipo`/`enum_tarefas_prioridade` + 6 FKs. Seed rodado (Tráfego id 1 / Criativos id 2).

**Decisões:** status POR LISTA (não enum global); sem recorrência no v1; view "lista" (não kanban por arrasto); cliente opcional; time inteiro vê todas as tarefas (sem escopo de carteira no v1). Minors deferidos: 500-vs-400 em enum inválido no endpoint, sem confirmação de exclusão na UI, teste HTTP 403 do endpoint.

**Reforma de UI (06/ago, branch feat/tarefas-ui → main aa79998, NO AR):** trocou o `TarefasLista` cru por **`TarefasBoard`** (client) — card-padrão branco/azul com **toggle Kanban↔Lista** (padrão de todas as tarefas), avatares dos responsáveis (`AvatarPessoa` + `fotosDosUsuarios`), chip do cliente no card, drag→mover status. **`TarefaModal`** (detalhe editável + **comentários** estilo ClickUp + **cliente digitável** `CampoClienteBusca`). **Comentários:** campo `tarefas.anotacoes` (jsonb, DDL manual em prod) + ação `anotar` no `/acao` (autor = req.user carimbado no servidor, append cap ~50); `TarefaCard.anotacoes`. `OpcoesTarefas` mora em `TarefaModal.tsx`. Feito via subagent-driven (5 tasks + review final MERGE). Fixes: cliente stale no autocomplete, drag cross-lista bloqueado no Hub (valida slug contra a lista da tarefa). Página CLARA (≠ comercial escuro).

**v2 (06/ago, branch feat/tarefas-v2 → main 9821de3, NO AR):** subagent-driven, 10 tasks + review final opus (READY TO MERGE). (A) **Lista em linhas** compactas (grid 6 colunas: Tarefa·Empresa·Responsáveis·Criado por·Criada·Vencimento; atrasada em vermelho + ícone; divisórias 0.5px); (B) card Kanban com contorno azul claro `#9dc3f7`; (C) aba Tarefas removida do Comercial (só Tráfego); (D) **barra de filtros** `TarefasFiltros` (URL params `de/ate/cliente/resp/autor/etq/prio/eu`; Modo EU = `meuId` do `req.user` server-side; etiqueta filtrada em memória, dropdown calculado ANTES do filtro); (E) campo **`etiquetas`** jsonb + ação `etiquetas` no `/acao` (trim/dedupe/cap 15) + chips; (F) `CampoResponsavelBusca` no modal (autocomplete hasMany); (G) **EditorStatus** self-service (endpoint `/api/listas-tarefas/gerir`, supervisor+, slug estável no rename, delete bloqueado se houver tarefa no status, ≥1 status) — botão "Gerenciar status" só no menu (nunca no Hub/união). Datas `de/ate` validadas por regex antes do where (URL adulterada dava 500). DDL prod: só `ALTER TABLE tarefas ADD COLUMN etiquetas jsonb` (não mexe em tabelas compartilhadas).

**Minors deferidos (v2):** `concluidaEm` não re-deriva quando supervisor muda o `tipo` de um status em uso; teste int de etiquetas não exercita o dedupe via endpoint; input color exige hex 6 dígitos.

**Ajustes v2.1 (07/ago, → main 5c01f37, NO AR):** board na LARGURA TOTAL (caiu o maxWidth 1100 da TarefasView) + **recolher/expandir por status** (chevron no cabeçalho; Lista esconde a seção, Kanban vira barra vertical estreita de 46px com contador que ainda aceita drop; estado por sessão, chaveado por slug em Set). Gerenciar status confirmado como está (admin+supervisor+administrativo). No merge veio o fix do chat paralelo 7ce2b71: preferências gravam SEM payload.update (deslogava — ver [[reference_payload_update_sessions]]).

**v2.2 (07/ago, → main a7e4a2b, NO AR):** etiquetas — qualquer equipe ADICIONA, excluir só supervisor+ (trava no servidor: ações `etiquetas` e `editar` comparam com as atuais via `etiquetasRemovidas` de src/lib/tarefas/etiquetasRegra.ts, 403 na remoção sem permissão; UI esconde o X via prop `podeExcluirEtiqueta` nas views). + fix: campo Anotações do Hub/Resumo sem background caía no cinza default do navegador em dark (só esse call-site do MencaoTextarea faltava).

**Parte 3 = PERFIL DA PESSOA + @menção CLICÁVEL — NO AR (07/ago; verificar: `curl -s -o /dev/null -w '%{http_code}' 'https://membros.quirkgrowth.com.br/api/pessoas/perfil?id=1'` → 403=vivo).** Onda 1 de 2. Branch feat/perfil-pessoa (SDD, 6 tasks + review opus "Ready to merge") rebaseada 2× e mergeada → main `679d3c3` (sem DDL — tudo derivado). Spec/plano `docs/superpowers/{specs,plans}/2026-08-07-perfil-pessoa-mencao-clicavel*`. **`@Nome` vira chip clicável** nos 3 comentários (`TextoComMencoes` via `segmentarMencoes` + `useEquipe`; degrada pra texto puro se equipe vazia) → **popover** (`PerfilPopover`+provider: avatar/role/email/telefone + Ver perfil + Chat[em breve]) → **página `/admin/pessoa?id=`** (`PessoaView`) com tarefas (aberto/concluídas) + atividade recente. Endpoint `GET /api/pessoas/perfil` + `carregarPerfil` (lib) — **gate DUPLO `ehEquipe`** (chamador + alvo), equipe-only, isolamento hermético. **Onda 2 = chat de DM** (mensageria, coleção nova → `payload-migracao-prod`) — spec própria. **BUG corrigido (07/ago, deploy `3ab0dff`):** a página `/admin/pessoa` abria EM BRANCO — a view `PessoaView` foi registrada no `payload.config` mas o `importMap.js` não foi regenerado, então o Payload não resolvia o componente (rota existia = não era 404). Fix = `payload generate:importmap` + deploy. Ver o gotcha generalizado em [[project-area-membros-quirk]] (componente OU view custom sempre exige regenerar o importMap). **VERIFICADO pelo Renan (07/ago):** clique→popover→página funcionando sem branco. Onda 1 fechada. **Decisões/follow-ups:** feed de atividade é só-notificação (spec previa incluir eventos de tarefa; `montarAtividade(notifs,pessoaId)`; union 'tarefa-criada' é dead code); perfil é EQUIPE-WIDE não por-carteira (gestor vê tarefas/clientes de outro membro — não fere regra de ouro, é visibilidade intra-equipe); minors (id ausente→403; ROTULO_ROLE duplicado). **@menção clicável só nos comentários** (fora deles, estender depois reusando o popover).

**Parte 4 = CHAT DE DM da equipe (Onda 2) — NO AR (07/ago, deploy `6be1f9d`; verificar: `curl -s -o /dev/null -w '%{http_code}' https://membros.quirkgrowth.com.br/api/chat/resumo` → 403=vivo).** Onda 2 do perfil. Branch feat/chat-dm-equipe (SDD, 8 tasks + review final opus "Ready with fixes" → fix aplicado), rebaseada em origin/main (75fd430, que já tinha a feature CRM Conversas + coleção `crm-mensagens`) e mergeada. Spec/plano `docs/superpowers/{specs,plans}/2026-08-07-chat-dm-equipe*`. **Decisões:** só 1:1 (grupos depois), só texto (anexos depois), polling ~5s (sem websocket), ícone de chat próprio com badge (separado do sino), COM "visto". **2 coleções:** `chat-conversas` (denormalizada: `chave` única `menorId:maiorId` anti-duplicata, `participantes` hasMany, `ultimaMensagem{Texto,Em,Autor}` pra lista barata, `leituras` jsonb por-userId pro badge+visto) + `chat-mensagens` (append-only). Ambas `read` filtrado ao participante + `create/update/delete: nunca` (write só por endpoint). **DDL APLICADA em prod** (endpoint direto, ANTES do deploy) reconciliada 1:1 com o schema real do Payload via introspecção do banco de teste pós-push: `autor_id`/`conversa_id` são NOT NULL + **ON DELETE SET NULL** (não CASCADE — Payload gera assim; usuário/conversa nunca é hard-deletado), + Payload auto-indexa created_at/updated_at e as FKs + colunas de lock `chat_conversas_id`/`chat_mensagens_id`. **4 endpoints** `/api/chat/{resumo,mensagens,enviar,ler}` gated `ehEquipe` (403 fail-closed); `enviar` com **duplo-gate** (paraId também tem que ser equipe — nunca DM com cliente) + acha-ou-cria idempotente sob corrida (índice único na chave) + `marcarLido` via `jsonb_set` (não sobrescreve o outro participante, padrão `pool.query`). **UI:** `IconeChat`+badge no `AvatarConta` (polling 5s pausa com aba oculta), `ChatDrawer` global (uma instância, escuta o **`chatBus`** — emissor de MÓDULO, não contexto React, resolve o popover viver em árvore separada), `ChatThread` compartilhado (bolha minha/dele, "Visto HH:MM" só na última minha, dedup por id no append, links clicáveis via `segmentarLinks`), página `/admin/chat` (`ChatView` espelha PessoaView + **importMap regenerado**, menu `ITEM_CHAT` em menuAdmin.ts grupos TRAFEGO), botão **Chat** do popover da Onda 1 agora liga `chatBus.abrirChat(pessoa.id)`. **Isolamento HERMÉTICO** confirmado no review final (4 camadas: endpoints ehEquipe / read filtrado / duplo-gate no enviar / roster só-equipe). **int test 2/2 verde** (idempotência + jsonb_set contra banco real) + 36/36 unit. **Ondas futuras:** grupos, anexos (R2+isolamento Media), menção @ dentro do chat, editar/apagar, reações, busca, SSE se polling incomodar. **Minors/follow-ups:** `depoisDe` sem validação ISO (pode 500, equipe-gated); página cheia /admin/chat sem "nova conversa" (só o drawer inicia); comercial vê ícone/drawer mas não o item de menu (alcança /admin/chat por URL); `outroParticipante` dead-code; catch de acharOuCriarConversa engole erro; preview sem lock otimista. **BUG pós-deploy corrigido (08/ago, deploy `2baa501`):** clicar **enviar no drawer não mandava nada** (tabelas vazias, front engolia). Diagnóstico ao vivo no Chrome (claude-in-chrome): o endpoint funcionava 100% (fetch direto = 200), o front montava o `paraId` certo, MAS clicar o botão só disparava CLICK, nunca SUBMIT. **Raiz:** o `ChatDrawer` é montado no `AvatarConta`, que o Payload renderiza **dentro de `<a href="/admin/account">`**; todo clique no drawer borbulha até essa âncora, cujo handler de navegação SPA chama `preventDefault()` — o que **cancela o submit NATIVO do form** do composer. `requestSubmit()` funcionava (confirmou a raiz); `stopPropagation` no drawer também. **Fix em 2 etapas:** `2baa501` portal pro body (higiene, MAS insuficiente — eventos sintéticos do React borbulham pela árvore de COMPONENTES, não pelo DOM, então o clique ainda alcançava o onClick da âncora) → `c8b8a7c` **`onClick={(e)=>e.stopPropagation()}` no `<aside>` do drawer** (o que de fato resolve). Ver o gotcha em [[project-area-membros-quirk]] (pegadinha do portal + React synthetic events). **VERIFICADO AO VIVO (08/ago, claude-in-chrome na sessão do Renan):** envio funciona (clique real → fetch paraId certo → bolha renderiza → campo limpa), conversa bidirecional real (Renan↔Renan DM 519). **UX de descoberta (`47a22ee`):** o lápis não deixava claro como achar as pessoas e a página cheia nem tinha isso — adicionado botão **"Nova mensagem"** sempre visível no topo da lista + header "Para quem?" com voltar + vazio que convida, no drawer E na página (`ChatCentral` ganhou o picker `useEquipe` que só o drawer tinha). Onda 2 fechada e no ar.

**PENDENTE (v1/v2):** verificação user-facing (login de equipe do Renan em /admin/tarefas + aba do Hub).

**Aba Concluídas + histórico sob demanda + recorrência diária + link do cliente (11/ago, branches feat/tarefas-aba-concluidas → main d3dfe59, feat/tarefas-recorrencia-diaria → main f46841b, todas NO AR):** três entregas seguidas na mesma sessão.
- **Aba Concluídas:** o board (Kanban/Lista, `/admin/tarefas` E aba Tarefas do Hub) parou de carregar tarefas concluídas por padrão — `montarWhereTarefas` sempre filtra por `concluidaEm` (`exists:false`=ativas, `exists:true`=concluídas; único sinal, nunca resolve slug de status). Toggle **"Ver concluídas (N)"** ↔ **"← Voltar às ativas"** em `TarefasBoard` via `?concluidas=1` na URL (mesmo padrão dos outros filtros). A aba Concluídas NÃO embute `historico`/`anotacoes` na listagem (evita o custo que motivou o projeto todo — board crescia sem limite com tarefa concluída antiga carregando histórico inteiro); busca sob demanda ao abrir o card via nova ação `acao:'detalhe'` no `/api/tarefas/acao` (`TarefaModal` ganhou prop `precisaDetalhe`). Kanban/Lista filtram as COLUNAS por `status.tipo==='concluido'` (ativas mostram só aberto/andamento, concluídas só concluido/cancelado) — **efeito colateral aceito conscientemente**: sumiu o drag-pra-concluir no board ativo (completar agora é só via dropdown de Status no modal); revisão final pegou isso mas era exatamente o que o mockup aprovado já mostrava. **Tráfego abre em formato Lista por padrão** pra quem nunca escolheu Kanban/Lista explicitamente (`listas-tarefas.formatoInicial`, campo texto validado em código — não select — default `'kanban'`; `modoDeAbertura` ganhou 3º parâmetro opcional). DDL prod: só `listas_tarefas.formato_inicial` (+ UPDATE pontual pra Tráfego='lista', lista já existia). 7 tasks via SDD, review final (opus) achou 3 Important — 1 já era decisão aprovada (não-bug), 1 reforçou só o gate DDL-antes-do-push, 1 corrigida (aba Concluídas ordenava por `prazo`, chave ruim pra tarefa concluída — virou `-concluidaEm`); consertos bundled (`overrideAccess` explícito nos counts novos, count+lista em `Promise.all`, "+Nova tarefa" some na aba Concluídas).
- **Recorrência diária:** `RECORRENCIAS` ganhou `'diaria'` (pula sábado/domingo — só essa cadência precisa dessa regra; semanal/quinzenal/mensal inalteradas). `avancarUmDiaUtil` em `src/lib/tarefas/recorrencia.ts`. Sem DDL (campo já validava em código, genérico).
- **Link do cliente:** a pílula com o nome do cliente (Kanban e Lista) virou `<a href="/admin/clientes-hub?id=">` com `stopPropagation` (não abre o modal da tarefa por baixo) quando há `clienteId`.
- **GOTCHA achado nessa sessão (não relacionado, mas bloqueia verificação visual):** ver [[reference_modeloscontrato_corpo_orfa]] — coluna órfã trava `npm run dev` local num prompt de DATA LOSS. E [[reference_git_auth_ssh]] — credencial HTTPS quebrou no meio da sessão, origin trocado pra SSH.

**Parte 2 = CAIXA DE ENTRADA da equipe — NO AR (06/ago; verificar: `curl -s -o /dev/null -w '%{http_code}' https://membros.quirkgrowth.com.br/api/notificacoes/painel` → 403=vivo).** Branch feat/caixa-entrada-equipe (15 commits) rebaseada em origin/main → `bf2d901`, deploy via `git push HEAD:main`. Spec/plano `docs/superpowers/{specs,plans}/2026-08-06-caixa-entrada-equipe*`. Feito via SDD (8 tasks + review por task + review final opus "Ready with fixes" — **isolamento AIRTIGHT**). **Coleção `notificacoes`** (equipe-only, `read` só do próprio destinatário; **DDL APLICADA em prod**: enums nomeados + coluna de lock `payload_locked_documents_rels.notificacoes_id`). **Gatilhos:** atribuição (novo responsável em tarefa, diff, sem auto-notificar) + **@-menção** nos comentários de Tarefas/Anotações-cliente/CRM-lead (gated `ehEquipe` SERVER-SIDE — member NUNCA dispara/recebe/vê; no CRM usa o `role` real, não o gate de negócio). **Endpoints** `/api/notificacoes/{painel,acao,equipe}` fail-closed 403. **Sino+painel** no `AvatarConta` (topo direita, 2 seções: Novidades não-lidas + Atribuídas a mim; item→marca lida+deep-link; "ver todas"→/admin/tarefas?eu=1). **Typeahead `@`** só-equipe (`MencaoTextarea`, degrada pra textarea puro no 403). PENDENTE: verificação user-facing do Renan logado. FOLLOW-UPS (não bloqueiam; ledger `.superpowers/sdd/progress.md`): parseMencoes Ana/Ana-Paula casa 2; usuariosEquipe sem filtro `ativo`; deep-link anotacao sem `&aba`; worklist não filtra concluído; novidades não ordena não-lida-primeiro. INFRA: `DATABASE_URI_TEST` compartilhado entre worktrees trava `tests/int` no drizzle push — isolar por worktree.

**Checklist + Subtarefa completa (14/ago, NO AR — deploys `61b930f`→`c179590` na mesma sessão):**
- **Checklist leve** (`tarefas.subtarefas` jsonb `{id,titulo,feito}[]`, DDL aplicado): seção no `TarefaModal`. **TROCA (15/ago, `c4ff034`): checklist virou 100% INTERNA da task** — o `ChecklistInline` do board foi removido (Renan preferiu: subtarefas ficam com a presença no board; checklist só dentro do modal). Histórico: nasceu visível no board minimizada ("▶ ☑ X/N"). Helpers puros `normalizarSubtarefas`/`progressoSubtarefas` em `src/lib/tarefas/subtarefas.ts`; ação `subtarefas` no `/acao` (manda a lista inteira, server normaliza, SEM histórico). Checkbox custom `CaixaCheck` (azul Quirk + ✓; nativo destoava no navy) com stopPropagation embutido. **BUG de UX pego pelo Renan em prod:** item só entrava com Enter — digitar e clicar fora descartava em silêncio ("não salva"); diagnóstico provou o server OK via script reversível; fix = adicionar TAMBÉM no blur + espelho síncrono `useRef` (closure de render perdia item em blur+clique em sequência). LIÇÃO: input "adicionar" deve commitar no blur, não só no Enter.
- **Subtarefa completa** (estilo ClickUp, print do Renan como referência): campo `tarefaPai` (auto-relationship, DDL `tarefa_pai_id` FK+índice aplicado). Task filha DE VERDADE (status/responsáveis/prazo/descrição/comentários), **aninhada no pai** (nunca card solto): `semFilhas: true` nos boards (PessoaView NÃO passa — filha aparece na lista por responsável), `listarFilhas`/`agruparPorPai` na consulta (filhas vêm SEM anotacoes/historico → modal usa `precisaDetalhe`). Lista = chevron ▶ + "⤷ N" no pai, filhas em linhas indentadas do MESMO grid; Kanban = bloco "▶ ⤷ N subtarefas" no card. Modal do pai: seção Subtarefas + "+ Subtarefa" (Enter/blur); modal da filha: breadcrumb "⤴ pai" + select de lista travado (herdada). **Subtarefa NÃO expõe status de funil (15/ago, `af94307`, pedido do Renan — "subtasks não devem ter status"):** a bolinha colorida saiu de todo lugar; no lugar, `CheckConclusaoFilha` (CaixaCheck que move pro 1º status tipo `concluido` da lista ao marcar / 1º não-concluído ao desmarcar, via ação `mover` — o hook deriva `concluidaEm`). Modal da filha: seletor de Status escondido, vira check "Concluída". O status continua existindo POR BAIXO (necessário pro concluidaEm) — só não é exposto. Server: `criar` c/ `tarefaPaiId` valida 1 NÍVEL e herda lista/cliente do pai; `excluir` do pai cascateia filhas em código (FK é SET NULL). **Modal keyed por `aberta?.id`** — sem o key, navegar pai↔filha não resetava o rascunho (useState só-de-mount). Refactor: `docParaCard` extraído do map inline do `listarTarefas`. Conclusão pai/filha independente; checklist e subtarefas convivem.
- **Departamento SOCIAL MEDIA (15/ago, `07bfd6e`, NO AR):** lista "Social Media" (id 6 em prod) com **14 status espelhando o ClickUp** + papel `social` DENTRO de Tarefas — detalhes do gate/fail-closed/enum em [[reference_papel_social_media]]. Dinâmica igual aos outros setores: task pessoal ou de cliente (cliente marcado → aba Tarefas do Hub).
- **KICKOFF REDESENHADO (15/ago, `7f66126`, NO AR):** "Iniciar Kickoff" (Hub→Tarefas) agora cria **1 TAREFA por template com os itens como CHECKLIST** (`montarTarefaKickoff` substituiu `montarTarefasDoChecklist` — antes eram 27 tarefas soltas), nascendo na lista **TRÁFEGO** (templates reapontados em prod + seed). Episódio: o clique de teste do Renan "não criou nada" — na verdade criou 27 soltas na Administrativo (destino do reponte de ontem); apagadas (ids 151-177, cliente teste Vitacon) via `scripts/kickoff-trafego-limpeza.mts`. + **Placar da checklist FORA da task (`befac7d`):** chip "☑ X/N" (verde quando completa) + barrinha no card Kanban e chip na linha da Lista — itens continuam internos (a "troca" segue valendo). + **FIX sobreposição no modal (`705f25f`):** o wrapper do `CampoDescricaoRica` tinha `flex:1`+`minHeight:0` e ENCOLHIA abaixo do conteúdo com checklist longa (toolbar vazava por cima dos itens) — virou `flex: '1 0 auto'` + separadores (borderTop) nas seções Checklist/Subtarefas. **PENDENTE (aguardando aprovado do Renan): seleção múltipla de tasks** (checkbox + barra: mover status / enviar pra outra lista / copiar / excluir + blindagem server-side do social por lista da tarefa).
- **Lista "Onboarding" REMOVIDA (15/ago, `a1c7cd9`, pedido do Renan):** a 5ª lista criada na migração de templates do ClickUp não devia existir — onboarding vive na **Administrativa**. Em prod: estava vazia; templates Kick Off Meta/Google reapontados `listaAlvo`→Administrativo (id 3) ANTES de excluir; gatilho da assinatura já usava 'Administrativo' por nome. Código: entrada removida do `LISTAS_SEED`, `seedTarefaTemplatesKickOff` agora exige a Administrativa (não cria Onboarding), teste ajustado pra 4 listas. Ficaram: Tráfego, Criativos, Administrativo, CS.
